/* Generated by scripts/build-production-bundles.sh. Edit the source modules, then rebuild. */

/* ===== Source: navegacao-arena-v2.js ===== */
(() => {
  'use strict';

  const PRIMARY_PAGES = ['home', 'tournament', 'registrations', 'champions', 'teams', 'community'];
  const SECONDARY_PAGES = ['news', 'history', 'flash', 'season', 'feedback'];
  const MOBILE_PRIMARY_PAGES = ['home', 'tournament', 'registrations', 'champions'];
  const MOBILE_SECONDARY_PAGES = ['news', 'history', 'flash', 'season', 'teams', 'community', 'feedback'];
  const ORDER = [...PRIMARY_PAGES, ...SECONDARY_PAGES];
  const HISTORY_PAGE_KEY = 'arenaNavigationPage';
  const HISTORY_DEPTH_KEY = 'arenaNavigationDepth';
  const META = {
    home: ['01', 'Início', 'Visão geral da arena'],
    tournament: ['02', 'Campeonatos', 'Copas, ligas e confrontos'],
    registrations: ['03', 'Inscrições', 'Vagas e aprovações'],
    champions: ['04', 'Campeões', 'Sala de troféus'],
    teams: ['05', 'Times', 'Clubes cadastrados'],
    community: ['06', 'Comunidade', 'Arquibancada do clã'],
    news: ['07', 'Notícias', 'Comunicados e novidades'],
    history: ['08', 'História', 'Memória oficial do clã'],
    flash: ['09', 'Copas Flash', 'Edições rápidas'],
    season: ['10', 'Temporada', 'Calendário e classificação'],
    feedback: ['11', 'Feedback', 'Ajude a melhorar a Arena']
  };
  const ICONS = {
    home: '<svg viewBox="0 0 24 24"><path d="M3 10.5 12 3l9 7.5"/><path d="M5.5 9.5V21h13V9.5M9 21v-6h6v6"/></svg>',
    tournament: '<svg viewBox="0 0 24 24"><path d="M8 4h8v3c0 4-1.8 7-4 7S8 11 8 7V4Z"/><path d="M8 6H5v1c0 3 1.6 5 4.2 5M16 6h3v1c0 3-1.6 5-4.2 5M12 14v4M8 21h8M9 18h6"/></svg>',
    registrations: '<svg viewBox="0 0 24 24"><rect x="5" y="4" width="14" height="17" rx="2"/><path d="M9 4V2h6v2M8.5 12l2.5 2.5 4.5-5"/></svg>',
    champions: '<svg viewBox="0 0 24 24"><path d="m12 3 2.6 5.3 5.9.9-4.3 4.2 1 5.9-5.2-2.8-5.2 2.8 1-5.9-4.3-4.2 5.9-.9L12 3Z"/></svg>',
    teams: '<svg viewBox="0 0 24 24"><path d="m12 3 8 3v5c0 5.2-3.4 8.6-8 10-4.6-1.4-8-4.8-8-10V6l8-3Z"/><path d="M9 12h6M12 9v6"/></svg>',
    community: '<svg viewBox="0 0 24 24"><circle cx="9" cy="9" r="3"/><circle cx="17" cy="10" r="2.5"/><path d="M3.5 20c.4-3.6 2.2-5.5 5.5-5.5s5.1 1.9 5.5 5.5M14 15.5c.8-.5 1.8-.7 2.9-.7 2.5 0 3.9 1.6 4.1 4.5"/></svg>',
    news: '<svg viewBox="0 0 24 24"><rect x="4" y="4" width="16" height="16" rx="2"/><path d="M8 8h8M8 12h8M8 16h5"/></svg>',
    history: '<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3.5 2"/></svg>',
    flash: '<svg viewBox="0 0 24 24"><path d="m13.5 2-8 12H12l-1.5 8 8-12H12l1.5-8Z"/></svg>',
    season: '<svg viewBox="0 0 24 24"><rect x="4" y="5" width="16" height="15" rx="2"/><path d="M8 3v4M16 3v4M4 10h16M8 14h2M14 14h2"/></svg>',
    feedback: '<svg viewBox="0 0 24 24"><path d="M4 5h16v12H9l-5 4V5Z"/><path d="m9 11 2 2 4-4"/></svg>',
    more: '<svg viewBox="0 0 24 24"><path d="M5 7h14M5 12h14M5 17h14"/></svg>',
    notifications: '<svg viewBox="0 0 24 24"><path d="M6.5 9a5.5 5.5 0 0 1 11 0c0 6 2.5 6 2.5 8H4c0-2 2.5-2 2.5-8Z"/><path d="M10 20h4"/></svg>',
    settings: '<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.7 1.7 0 0 0 .3 1.9l.1.1-2.8 2.8-.1-.1a1.7 1.7 0 0 0-1.9-.3 1.7 1.7 0 0 0-1 1.6v.2h-4V21a1.7 1.7 0 0 0-1-1.6 1.7 1.7 0 0 0-1.9.3l-.1.1L4.2 17l.1-.1a1.7 1.7 0 0 0 .3-1.9A1.7 1.7 0 0 0 3 14H2.8v-4H3a1.7 1.7 0 0 0 1.6-1 1.7 1.7 0 0 0-.3-1.9L4.2 7 7 4.2l.1.1a1.7 1.7 0 0 0 1.9.3A1.7 1.7 0 0 0 10 3V2.8h4V3a1.7 1.7 0 0 0 1 1.6 1.7 1.7 0 0 0 1.9-.3l.1-.1L19.8 7l-.1.1a1.7 1.7 0 0 0-.3 1.9 1.7 1.7 0 0 0 1.6 1h.2v4H21a1.7 1.7 0 0 0-1.6 1Z"/></svg>',
    search: '<svg viewBox="0 0 24 24"><circle cx="10.5" cy="10.5" r="6.5"/><path d="m15.5 15.5 4.5 4.5"/></svg>',
    share: '<svg viewBox="0 0 24 24"><path d="M14 5h5v5M19 5l-8 8"/><path d="M18 13v6H5V6h6"/></svg>',
    arrowRight: '<svg viewBox="0 0 24 24"><path d="M5 12h14M14 7l5 5-5 5"/></svg>'
  };

  const $ = (selector, root = document) => root.querySelector(selector);
  const $$ = (selector, root = document) => [...root.querySelectorAll(selector)];
  const iconUrl = () => $('link[rel~="icon"]')?.href || './favicon.svg';
  const currentPage = () => $('.page.active')?.dataset.page || 'home';
  const navIcon = page => ICONS[page] || ICONS.more;
  window.ArenaBDAIcons = Object.freeze({ get: navIcon });

  let mobileNav;
  let sheet;
  let pageBackButton;
  let viewportFrame = 0;

  function normalizePage(page) {
    return ORDER.includes(page) && $(`.page[data-page="${page}"]`) ? page : 'home';
  }

  function historyDepth() {
    const value = Number(window.history.state?.[HISTORY_DEPTH_KEY]);
    return Number.isInteger(value) && value > 0 ? value : 0;
  }

  function routeUrl(page) {
    const url = new URL(window.location.href);
    url.hash = page === 'home' ? '' : page;
    return `${url.pathname}${url.search}${url.hash}`;
  }

  function routeState(page, depth) {
    const currentState = window.history.state;
    return {
      ...(currentState && typeof currentState === 'object' ? currentState : {}),
      [HISTORY_PAGE_KEY]: page,
      [HISTORY_DEPTH_KEY]: depth
    };
  }

  function syncPageBack() {
    if (!pageBackButton) return;
    const visible = historyDepth() > 0 || currentPage() !== 'home';
    pageBackButton.hidden = !visible;
    document.body.classList.toggle('arena-has-page-back', visible);
  }

  function installHistoryNavigation() {
    if (window.navigate?.arenaHistoryEnabled) return;

    const renderPage = typeof window.navigate === 'function'
      ? window.navigate.bind(window)
      : page => {
          $$('.page').forEach(item => item.classList.toggle('active', item.dataset.page === page));
          window.scrollTo({ top: 0, behavior: 'smooth' });
        };
    const requestedPage = normalizePage(window.location.hash.slice(1));

    const navigateWithHistory = (page, options = {}) => {
      const targetPage = normalizePage(page);
      const activePage = currentPage();
      const fromHistory = options?.fromHistory === true;

      if (!fromHistory && targetPage !== activePage) {
        const depth = historyDepth();
        if (options?.replace === true) {
          window.history.replaceState(routeState(targetPage, depth), '', routeUrl(targetPage));
        } else {
          window.history.pushState(routeState(targetPage, depth + 1), '', routeUrl(targetPage));
        }
      }

      renderPage(targetPage);
      closeSheet();
      requestAnimationFrame(() => {
        syncActive();
        syncPageBack();
      });
    };

    navigateWithHistory.arenaHistoryEnabled = true;
    window.navigate = navigateWithHistory;

    window.history.replaceState(routeState('home', 0), '', routeUrl('home'));
    renderPage('home');
    if (requestedPage !== 'home') {
      window.history.pushState(routeState(requestedPage, 1), '', routeUrl(requestedPage));
      renderPage(requestedPage);
    }

    window.addEventListener('popstate', event => {
      const statePage = event.state?.[HISTORY_PAGE_KEY];
      const hashPage = window.location.hash.slice(1);
      navigateWithHistory(normalizePage(statePage || hashPage), { fromHistory: true });
    });
  }

  function buildPageBack() {
    const topbar = $('.topbar');
    if (!topbar) return;

    pageBackButton = $('.arena-page-back', topbar);
    if (!pageBackButton) {
      pageBackButton = document.createElement('button');
      pageBackButton.type = 'button';
      pageBackButton.className = 'arena-page-back';
      pageBackButton.setAttribute('aria-label', 'Voltar para a página anterior');
      pageBackButton.innerHTML = '<i aria-hidden="true"><svg viewBox="0 0 24 24"><path d="m14.5 5-7 7 7 7"/><path d="M8 12h10"/></svg></i><span>Voltar</span>';
      pageBackButton.addEventListener('click', () => {
        if (historyDepth() > 0) window.history.back();
        else window.navigate('home', { replace: true });
      });
      topbar.prepend(pageBackButton);
    }
    syncPageBack();
  }

  function go(page) {
    if (typeof navigate === 'function') navigate(page);
    else {
      $$('.page').forEach(item => item.classList.toggle('active', item.dataset.page === page));
      window.scrollTo({ top: 0, behavior: 'smooth' });
    }
    closeSheet();
    requestAnimationFrame(syncActive);
  }

  function decorate(button) {
    if (!button) return;
    const meta = META[button.dataset.go];
    if (!meta || button.dataset.arenaDecorated === 'true') return;

    button.dataset.arenaDecorated = 'true';
    button.classList.add('arena-nav-item');
    button.title = `${meta[1]} • ${meta[2]}`;
    button.setAttribute('aria-label', meta[1]);
    button.innerHTML = `<i aria-hidden="true">${navIcon(button.dataset.go)}</i><span class="arena-nav-copy"><b>${meta[1]}</b><small>${meta[2]}</small></span>`;
  }

  function buildSidebar() {
    const nav = $('.bottom-nav');
    if (!nav) return;

    nav.classList.add('arena-side-nav');
    nav.setAttribute('aria-label', 'Navegação principal da Arena BDA');

    let brand = $('.arena-side-brand', nav);
    if (!brand) {
      brand = document.createElement('div');
      brand.className = 'arena-side-brand';
      brand.innerHTML = `<span class="arena-side-logo"><img src="${iconUrl()}" alt="Grifo da Arena BDA"></span><span><b>Arena BDA</b><small>Central do Clã</small></span>`;
      nav.prepend(brand);
    }

    let heading = $('.arena-side-heading', nav);
    if (!heading) {
      heading = document.createElement('div');
      heading.className = 'arena-side-heading';
      heading.textContent = 'Navegação';
      brand.after(heading);
    }

    const buttons = new Map($$('.nav-btn[data-go]', nav).map(button => [button.dataset.go, button]));
    ORDER.forEach(page => decorate(buttons.get(page)));

    const orderedButtons = ORDER.map(page => buttons.get(page)).filter(Boolean);
    const currentButtons = $$('.nav-btn[data-go]', nav);
    const isOrdered = orderedButtons.length === currentButtons.length && orderedButtons.every((button, index) => button === currentButtons[index]);
    if (!isOrdered) orderedButtons.forEach(button => nav.append(button));

    orderedButtons.forEach(button => {
      button.classList.toggle('arena-side-secondary', SECONDARY_PAGES.includes(button.dataset.go));
    });

    let more = $('.arena-side-more-toggle', nav);
    if (!more) {
      more = document.createElement('button');
      more.type = 'button';
      more.className = 'nav-btn arena-nav-item arena-side-more-toggle';
      more.innerHTML = `<i aria-hidden="true">${navIcon('more')}</i><span class="arena-nav-copy"><b>Mais</b><small>5 áreas</small></span>`;
      more.addEventListener('click', () => setSidebarMore(!nav.classList.contains('arena-side-more-open')));
    }
    more.setAttribute('aria-label', 'Mostrar mais áreas');
    const firstSecondary = orderedButtons.find(button => button.classList.contains('arena-side-secondary'));
    if (firstSecondary) firstSecondary.before(more);
    const secondaryGroup = orderedButtons.filter(button => button.classList.contains('arena-side-secondary'));
    secondaryGroup.forEach((button, index) => {
      button.id = `arenaSecondaryNavigation${index + 1}`;
    });
    more.setAttribute('aria-controls', secondaryGroup.map(button => button.id).join(' '));

    if (!$('.arena-side-footer', nav)) {
      const footer = document.createElement('div');
      footer.className = 'arena-side-footer';
      footer.innerHTML = '<div><b>Site oficial</b><small>arenabda.com.br</small></div>';
      nav.append(footer);
    }
  }

  function setSidebarMore(open) {
    const nav = $('.arena-side-nav');
    const more = $('.arena-side-more-toggle', nav || document);
    if (!nav || !more) return;
    nav.classList.toggle('arena-side-more-open', open);
    more.setAttribute('aria-expanded', open ? 'true' : 'false');
    more.setAttribute('aria-label', open ? 'Ocultar áreas adicionais' : 'Mostrar mais áreas');
  }

  function mobileItem(page) {
    const [icon, label] = META[page];
    const button = document.createElement('button');
    button.type = 'button';
    button.className = 'arena-mobile-item';
    button.dataset.mobileGo = page;
    button.innerHTML = `<i aria-hidden="true">${navIcon(page)}</i><span>${label}</span>`;
    button.setAttribute('aria-label', label);
    button.addEventListener('click', () => go(page));
    return button;
  }

  function buildMobile() {
    if (!mobileNav) {
      mobileNav = document.createElement('nav');
      mobileNav.className = 'arena-mobile-nav';
      mobileNav.setAttribute('aria-label', 'Navegação móvel');
      MOBILE_PRIMARY_PAGES.forEach(page => mobileNav.append(mobileItem(page)));

      const more = document.createElement('button');
      more.type = 'button';
      more.className = 'arena-mobile-item';
      more.dataset.mobileMore = 'true';
      more.innerHTML = `<i aria-hidden="true">${navIcon('more')}</i><span>Mais</span>`;
      more.setAttribute('aria-label', 'Abrir mais opções');
      more.addEventListener('click', openSheet);
      mobileNav.append(more);
      document.body.append(mobileNav);
    }

    if (!sheet) {
      sheet = document.createElement('div');
      sheet.className = 'arena-nav-sheet-backdrop';
      sheet.setAttribute('aria-hidden', 'true');
      sheet.innerHTML = `
        <section class="arena-nav-sheet" role="dialog" aria-modal="true" aria-label="Mais opções">
          <header><div><span class="eyebrow">Arena BDA</span><h2>Mais opções</h2></div><button type="button" class="arena-sheet-close" aria-label="Fechar">×</button></header>
          <div class="arena-sheet-grid"></div>
          <footer><img src="${iconUrl()}" alt="Grifo"><span><b>Clã BDA</b><small>Todos os campeonatos em uma só arena.</small></span></footer>
        </section>`;

      MOBILE_SECONDARY_PAGES.forEach(page => {
        const [icon, label, description] = META[page];
        const button = document.createElement('button');
        button.type = 'button';
        button.className = 'arena-sheet-item';
        button.dataset.sheetGo = page;
        button.innerHTML = `<i aria-hidden="true">${navIcon(page)}</i><span><b>${label}</b><small>${description}</small></span><em>›</em>`;
        button.addEventListener('click', () => go(page));
        $('.arena-sheet-grid', sheet).append(button);
      });

      $('.arena-sheet-close', sheet).addEventListener('click', closeSheet);
      sheet.addEventListener('click', event => event.target === sheet && closeSheet());
      document.body.append(sheet);
    }
  }

  function openSheet() {
    sheet?.classList.add('show');
    sheet?.setAttribute('aria-hidden', 'false');
    document.body.classList.add('arena-sheet-open');
    $('.arena-sheet-close', sheet)?.focus();
  }

  function closeSheet() {
    sheet?.classList.remove('show');
    sheet?.setAttribute('aria-hidden', 'true');
    document.body.classList.remove('arena-sheet-open');
  }

  function syncActive() {
    const page = currentPage();

    setSidebarMore(SECONDARY_PAGES.includes(page));

    $$('.arena-side-nav .nav-btn[data-go]').forEach(button => {
      const active = button.dataset.go === page;
      button.classList.toggle('active', active);
      active ? button.setAttribute('aria-current', 'page') : button.removeAttribute('aria-current');
    });

    $$('.arena-mobile-item[data-mobile-go]').forEach(button => {
      const active = button.dataset.mobileGo === page;
      button.classList.toggle('active', active);
      active ? button.setAttribute('aria-current', 'page') : button.removeAttribute('aria-current');
    });

    const more = $('.arena-mobile-item[data-mobile-more]');
    if (more) {
      const active = MOBILE_SECONDARY_PAGES.includes(page);
      more.classList.toggle('active', active);
      active ? more.setAttribute('aria-current', 'page') : more.removeAttribute('aria-current');
    }

    const sideMore = $('.arena-side-more-toggle');
    if (sideMore) {
      const active = SECONDARY_PAGES.includes(page);
      sideMore.classList.toggle('active', active);
      active ? sideMore.setAttribute('aria-current', 'page') : sideMore.removeAttribute('aria-current');
    }

    syncPageBack();
  }

  function syncMobileViewport() {
    viewportFrame = 0;
    if (!mobileNav) return;
    const viewport = window.visualViewport;
    const coveredHeight = viewport ? Math.max(0, window.innerHeight - viewport.height - viewport.offsetTop) : 0;
    const keyboardOpen = coveredHeight > 140;
    mobileNav.classList.toggle('keyboard-open', keyboardOpen);
    mobileNav.setAttribute('aria-hidden', keyboardOpen ? 'true' : 'false');
  }

  function scheduleViewportSync() {
    if (viewportFrame) return;
    viewportFrame = requestAnimationFrame(syncMobileViewport);
  }

  function init() {
    document.body.classList.add('arena-navigation-ready');
    installHistoryNavigation();
    buildPageBack();
    buildSidebar();
    buildMobile();
    syncActive();
    syncMobileViewport();
  }

  const style = document.createElement('style');
  style.textContent = `
    .arena-side-brand,.arena-side-heading,.arena-side-footer,.arena-mobile-nav,.arena-nav-sheet-backdrop{display:none}
    .topbar>.top-actions{margin-left:auto}
    .arena-page-back{display:inline-flex;align-items:center;justify-content:center;gap:7px;min-width:82px;height:40px;flex:0 0 auto;padding:0 11px;border:1px solid var(--line);border-radius:6px;color:var(--text);background:transparent;font-size:12px;font-weight:750;line-height:1;white-space:nowrap}
    .arena-page-back[hidden]{display:none}
    .arena-page-back:hover{border-color:rgba(242,215,125,.42);background:rgba(255,255,255,.04)}
    .arena-page-back:focus-visible{outline:2px solid var(--gold-soft);outline-offset:2px}
    .arena-page-back i{display:grid;place-items:center;font-style:normal}
    .arena-page-back svg,.arena-nav-item i svg,.arena-mobile-item i svg,.arena-sheet-item i svg{display:block;width:18px;height:18px;fill:none;stroke:currentColor;stroke-width:1.8;stroke-linecap:round;stroke-linejoin:round}

    @media(min-width:981px){
      html{scroll-padding-top:90px}
      body.arena-navigation-ready{display:block;min-height:100vh;padding:0 18px 0 252px;text-align:left}
      body.arena-navigation-ready .app-shell{width:100%;max-width:1320px;margin:0 auto}
      body.arena-navigation-ready .topbar{top:12px;margin:12px 0 0;border:1px solid var(--line);border-radius:19px;background:rgba(5,10,8,.88);box-shadow:0 12px 34px rgba(0,0,0,.28)}
      body.arena-navigation-ready main{padding:20px 4px 36px}
      .bottom-nav.arena-side-nav{position:fixed;left:14px;top:14px;bottom:14px;z-index:45;width:220px;height:auto;min-height:0;transform:none;display:flex;flex-direction:column;gap:6px;padding:13px;border:1px solid rgba(242,215,125,.25);border-radius:25px;background:radial-gradient(circle at 50% 0,rgba(216,178,72,.19),transparent 27%),linear-gradient(180deg,rgba(12,27,19,.98),rgba(4,10,7,.98));box-shadow:0 24px 60px rgba(0,0,0,.52);backdrop-filter:blur(22px);overflow-x:hidden;overflow-y:auto;scrollbar-width:thin;scrollbar-color:rgba(242,215,125,.28) transparent}
      .bottom-nav.arena-side-nav::-webkit-scrollbar{width:5px}.bottom-nav.arena-side-nav::-webkit-scrollbar-thumb{border-radius:999px;background:rgba(242,215,125,.28)}
      .arena-side-brand{display:flex;align-items:center;gap:11px;padding:8px 7px 16px;margin-bottom:2px;border-bottom:1px solid var(--line)}
      .arena-side-logo{overflow:hidden;width:48px;height:48px;flex:0 0 auto;border:1px solid rgba(242,215,125,.55);border-radius:15px;background:#020503;box-shadow:0 9px 24px rgba(216,178,72,.17)}
      .arena-side-logo img{display:block;width:100%;height:100%;object-fit:cover}
      .arena-side-brand b,.arena-side-brand small{display:block}.arena-side-brand b{font:800 21px/1 "Barlow Condensed",sans-serif;letter-spacing:.05em;text-transform:uppercase}.arena-side-brand small{margin-top:5px;color:var(--gold-soft);font-size:11px;font-weight:800;letter-spacing:.12em;text-transform:uppercase}
      .arena-side-heading{display:block;padding:8px 10px 3px;color:var(--muted);font-size:11px;font-weight:900;letter-spacing:.17em;text-transform:uppercase}
      .arena-side-nav .nav-btn{position:relative;display:grid;grid-template-columns:40px 1fr;align-items:center;justify-items:stretch;gap:10px;width:100%;min-height:54px;padding:7px 9px;border:1px solid transparent;border-radius:15px;color:var(--muted);background:transparent;text-align:left;transition:transform .16s ease,border-color .16s ease,background .16s ease,color .16s ease}
      .arena-side-nav .arena-side-secondary{display:none}.arena-side-nav.arena-side-more-open .arena-side-secondary{display:grid}
      .arena-side-nav .nav-btn:hover{transform:translateX(3px);color:var(--text);border-color:var(--line);background:rgba(255,255,255,.045)}
      .arena-side-nav .nav-btn i{display:grid;place-items:center;width:40px;height:40px;border:1px solid var(--line);border-radius:12px;background:rgba(255,255,255,.04);font-size:19px;line-height:1}
      .arena-nav-copy{display:block;min-width:0}.arena-nav-copy b,.arena-nav-copy small{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.arena-nav-copy b{font-size:12px}.arena-nav-copy small{margin-top:4px;color:var(--muted);font-size:11px;font-weight:500}
      .arena-side-nav .nav-btn.active{color:#171107;border-color:var(--gold);background:linear-gradient(135deg,var(--gold-soft),var(--gold));box-shadow:0 10px 24px rgba(216,178,72,.18)}.arena-side-nav .nav-btn.active i{border-color:rgba(23,17,7,.14);background:rgba(255,255,255,.28)}.arena-side-nav .nav-btn.active .arena-nav-copy small{color:rgba(23,17,7,.68)}.arena-side-nav .nav-btn.active:after{content:"";position:absolute;right:8px;width:5px;height:5px;border-radius:50%;background:#171107;box-shadow:0 0 0 4px rgba(23,17,7,.11)}
      .arena-side-footer{display:flex;align-items:center;gap:9px;margin-top:auto;padding:11px 9px;border:1px solid var(--line);border-radius:14px;background:rgba(255,255,255,.035)}.arena-side-footer>span{color:var(--green);font-size:12px;text-shadow:0 0 12px var(--green)}.arena-side-footer b,.arena-side-footer small{display:block}.arena-side-footer b{font-size:11px;text-transform:uppercase}.arena-side-footer small{margin-top:3px;color:var(--muted);font-size:11px}
    }

    @media(min-width:981px) and (max-height:740px){
      .bottom-nav.arena-side-nav{gap:3px;padding-block:9px}.arena-side-brand{padding-bottom:10px}.arena-side-logo{width:42px;height:42px}.arena-side-nav .nav-btn{min-height:47px;padding-block:4px}.arena-side-nav .nav-btn i{width:35px;height:35px;font-size:17px}.arena-nav-copy small{display:none}.arena-side-footer{padding-block:8px}
    }

    @media(max-width:980px){
      body.arena-navigation-ready{padding-bottom:calc(74px + env(safe-area-inset-bottom))}
      .bottom-nav.arena-side-nav{display:none}
      .arena-page-back{width:40px;min-width:40px;height:40px;padding:0;border-radius:6px}
      .arena-page-back span{position:absolute;width:1px;height:1px;overflow:hidden;clip-path:inset(50%);white-space:nowrap}
      .arena-mobile-nav{position:fixed;left:8px;right:8px;bottom:max(6px,env(safe-area-inset-bottom));z-index:48;display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:3px;height:62px;padding:5px;border:1px solid rgba(242,215,125,.20);border-radius:19px;background:linear-gradient(180deg,rgba(15,31,22,.97),rgba(5,12,8,.985));box-shadow:0 12px 34px rgba(0,0,0,.48);backdrop-filter:blur(14px);transition:opacity .16s ease,transform .16s ease,visibility .16s}
      .arena-mobile-nav.keyboard-open{opacity:0;visibility:hidden;pointer-events:none;transform:translateY(calc(100% + 18px))}
      .arena-mobile-item{display:grid;place-items:center;align-content:center;gap:3px;min-width:0;min-height:50px;padding:3px 2px;border:0;border-radius:13px;color:var(--muted);background:transparent;font-size:11px;font-weight:800;line-height:1}.arena-mobile-item i{display:grid;place-items:center;width:28px;height:26px;border-radius:9px;font-style:normal;font-size:16px;line-height:1}.arena-mobile-item span{overflow:hidden;width:100%;text-overflow:ellipsis;white-space:nowrap}.arena-mobile-item.active{color:#171107;background:linear-gradient(135deg,var(--gold-soft),var(--gold));box-shadow:0 6px 14px rgba(216,178,72,.15)}.arena-mobile-item.active i{background:rgba(255,255,255,.22)}
      .arena-nav-sheet-backdrop{position:fixed;inset:0;z-index:70;display:grid;align-items:end;padding:12px 12px max(12px,env(safe-area-inset-bottom));background:rgba(0,0,0,.68);opacity:0;visibility:hidden;backdrop-filter:blur(7px);transition:.2s}.arena-nav-sheet-backdrop.show{opacity:1;visibility:visible}.arena-nav-sheet{overflow-y:auto;width:min(100%,520px);max-height:calc(100dvh - 24px - env(safe-area-inset-bottom));margin:0 auto;padding:18px;border:1px solid rgba(242,215,125,.28);border-radius:24px;background:radial-gradient(circle at 90% 0,rgba(216,178,72,.18),transparent 31%),linear-gradient(155deg,#132a1d,#07100c);box-shadow:0 24px 70px rgba(0,0,0,.65);transform:translateY(18px);transition:.2s}.arena-nav-sheet-backdrop.show .arena-nav-sheet{transform:none}
      .arena-nav-sheet header{display:flex;align-items:center;justify-content:space-between;gap:12px;padding-bottom:13px;border-bottom:1px solid var(--line)}.arena-nav-sheet h2{margin:3px 0 0;font-size:27px;text-transform:uppercase}.arena-sheet-close{width:42px;height:42px;border:1px solid var(--line);border-radius:13px;color:var(--text);background:rgba(255,255,255,.05);font-size:24px}.arena-sheet-grid{display:grid;gap:8px;margin:13px 0}
      .arena-sheet-item{display:grid;grid-template-columns:44px 1fr auto;align-items:center;gap:10px;width:100%;min-height:62px;padding:8px 11px;border:1px solid var(--line);border-radius:16px;color:var(--text);background:rgba(255,255,255,.035);text-align:left}.arena-sheet-item i{display:grid;place-items:center;width:43px;height:43px;border:1px solid rgba(242,215,125,.25);border-radius:13px;background:rgba(216,178,72,.08);font-style:normal;font-size:21px}.arena-sheet-item b,.arena-sheet-item small{display:block}.arena-sheet-item b{font-size:11px}.arena-sheet-item small{margin-top:4px;color:var(--muted);font-size:11px}.arena-sheet-item em{color:var(--gold-soft);font-size:26px;font-style:normal}
      .arena-nav-sheet footer{display:flex;align-items:center;gap:10px;padding-top:12px;border-top:1px solid var(--line)}.arena-nav-sheet footer img{width:40px;height:40px;border:1px solid rgba(242,215,125,.4);border-radius:12px;object-fit:cover}.arena-nav-sheet footer b,.arena-nav-sheet footer small{display:block}.arena-nav-sheet footer b{font-size:11px;text-transform:uppercase}.arena-nav-sheet footer small{margin-top:3px;color:var(--muted);font-size:11px}body.arena-sheet-open{overflow:hidden}
    }

    @media(max-width:390px){.arena-page-back{width:38px;min-width:38px;height:38px}.arena-mobile-nav{left:5px;right:5px;gap:2px;padding-inline:4px}.arena-mobile-item{font-size:11px}.arena-mobile-item i{width:27px;height:25px;font-size:15px}}
    @media(prefers-reduced-transparency:reduce){.arena-mobile-nav,.arena-nav-sheet-backdrop{backdrop-filter:none}}
  `;
  document.head.append(style);

  document.addEventListener('click', event => {
    if (event.target.closest('[data-go], [data-mobile-go], [data-sheet-go]')) setTimeout(syncActive, 20);
  });
  document.addEventListener('keydown', event => {
    if (event.key !== 'Escape') return;
    closeSheet();
    setSidebarMore(false);
  });
  window.visualViewport?.addEventListener('resize', scheduleViewportSync, { passive: true });
  window.visualViewport?.addEventListener('scroll', scheduleViewportSync, { passive: true });
  window.addEventListener('resize', scheduleViewportSync, { passive: true });

  const pageObserver = new MutationObserver(syncActive);
  $$('.page').forEach(page => pageObserver.observe(page, { attributes: true, attributeFilter: ['class'] }));

  init();
})();

;

/* ===== Source: arena-visual-system.js ===== */
(() => {
  'use strict';

  const VERSION = '2026.08-unified-visual-system';
  const $ = (selector, root = document) => root.querySelector(selector);
  const $$ = (selector, root = document) => [...root.querySelectorAll(selector)];

  const PAGES = Object.freeze({
    home: { icon: '01', label: 'Início', description: 'Visão geral da Arena', color: '#e3c45f', rgb: '227,196,95' },
    news: { icon: '02', label: 'Notícias', description: 'Comunicados oficiais', color: '#8eb8cc', rgb: '142,184,204' },
    history: { icon: '03', label: 'História', description: 'Memória do Clã BDA', color: '#c69c6b', rgb: '198,156,107' },
    tournament: { icon: '04', label: 'Campeonatos', description: 'Copas, ligas e confrontos', color: '#e3c45f', rgb: '227,196,95' },
    flash: { icon: '05', label: 'Copas Flash', description: 'Edições rápidas', color: '#a998c2', rgb: '169,152,194' },
    season: { icon: '06', label: 'Temporada', description: 'Calendário e classificação', color: '#8aaec7', rgb: '138,174,199' },
    registrations: { icon: '07', label: 'Inscrições', description: 'Vagas e aprovações', color: '#83b99b', rgb: '131,185,155' },
    champions: { icon: '08', label: 'Campeões', description: 'Títulos da Arena BDA', color: '#e3c45f', rgb: '227,196,95' },
    teams: { icon: '09', label: 'Times', description: 'Clubes cadastrados', color: '#7fb5aa', rgb: '127,181,170' },
    community: { icon: '10', label: 'Comunidade', description: 'Arquibancada do clã', color: '#a69abf', rgb: '166,154,191' },
    feedback: { icon: '11', label: 'Feedback', description: 'Ajude a melhorar a Arena', color: '#85b5bf', rgb: '133,181,191' }
  });

  const interfaceIcon = name => window.ArenaBDAIcons?.get?.(name) || `<span>${PAGES[name]?.icon || ''}</span>`;
  const brandIconUrl = () => $('link[rel~="icon"]')?.href || './favicon.svg';

  document.documentElement.dataset.arenaDesign = VERSION;
  document.body.classList.add('arena-visual-system');

  const style = document.createElement('style');
  style.id = 'arenaVisualSystemStyles';
  style.textContent = `
    body.arena-visual-system{
      --arena-accent:#e3c45f;
      --arena-accent-rgb:227,196,95;
      --bg:#050a07;
      --bg-soft:#07100b;
      --surface:#0b1710;
      --surface-2:#0f1d15;
      --surface-3:#13241a;
      --line:rgba(226,236,229,.10);
      --line-strong:rgba(var(--arena-accent-rgb),.28);
      --shadow:none;
      --shadow-soft:none;
      --radius:16px;
      --radius-small:11px;
      --arena-ease:cubic-bezier(.22,.61,.36,1);
      --arena-gold:var(--arena-accent);
      --arena-gold-soft:#f1d97f;
      --arena-panel:var(--surface);
      --arena-border:var(--line);
      --arena-border-strong:var(--line-strong);
      --arena-muted:#9baaa0;
      --arena-green:#59d98b;
      background:#050a07;
    }

    body.arena-visual-system:before,
    body.arena-visual-system:after{display:none}
    body.arena-visual-system>.app-shell{position:relative;z-index:1}
    body.arena-visual-system .page.active{animation:none}

    body.arena-visual-system .topbar{
      min-height:68px;
      border-color:var(--line);
      border-radius:14px;
      background:#09140d;
      box-shadow:none;
      backdrop-filter:none;
    }
    body.arena-visual-system .topbar:after{display:none}
    body.arena-visual-system .brand-mark{
      border-color:rgba(var(--arena-accent-rgb),.25);
      border-radius:11px;
      background:#07100b;
      box-shadow:none;
    }
    body.arena-visual-system .brand-copy strong{font-size:21px;letter-spacing:.045em}
    body.arena-visual-system .arena-top-status{
      min-height:36px;
      border-radius:10px;
      background:transparent;
    }
    body.arena-visual-system .arena-top-status i{box-shadow:none}
    body.arena-visual-system .icon-btn,
    body.arena-visual-system .admin-btn{
      border-color:var(--line);
      border-radius:10px;
      background:#0d1a12;
      box-shadow:none;
    }
    body.arena-visual-system .icon-btn:hover,
    body.arena-visual-system .admin-btn:hover{border-color:var(--line-strong);transform:none}
    body.arena-visual-system .admin-btn.active{background:var(--arena-accent)}

    body.arena-visual-system main{padding-top:16px}
    body.arena-visual-system :is(.hero,.arena-hero,.rank-hero,.history-hero,.league-generator-head){
      border-color:var(--line);
      border-radius:18px;
      box-shadow:none;
    }
    body.arena-visual-system .hero{
      min-height:286px;
      padding:clamp(22px,4vw,32px);
      background:linear-gradient(115deg,#10261a 0,#08110c 64%);
    }
    body.arena-visual-system .hero:before{opacity:.10;filter:grayscale(.35) contrast(1.05)}
    body.arena-visual-system .hero:after{display:none}
    body.arena-visual-system .hero h1{
      max-width:760px;
      margin-top:8px;
      font-size:clamp(40px,7vw,62px);
      letter-spacing:-.02em;
    }
    body.arena-visual-system .hero p{font-size:11px;line-height:1.6}
    body.arena-visual-system .eyebrow{gap:7px;color:var(--arena-accent);letter-spacing:.13em}
    body.arena-visual-system .eyebrow:before{width:14px;height:1px;background:var(--arena-accent)}

    body.arena-visual-system :is(.primary,.secondary,.ghost,.danger){
      min-height:41px;
      border-radius:10px;
      box-shadow:none;
      transition:border-color .15s ease,background .15s ease,color .15s ease;
    }
    body.arena-visual-system .primary{background:var(--arena-accent)}
    body.arena-visual-system .secondary{background:#102018}
    body.arena-visual-system .ghost{background:transparent}
    body.arena-visual-system .danger{background:rgba(255,114,128,.08)}
    body.arena-visual-system :is(.primary,.secondary,.ghost,.danger):hover{transform:none;filter:none}

    body.arena-visual-system .section-head{
      margin-top:26px;
      margin-bottom:12px;
      padding-left:0;
    }
    body.arena-visual-system .section-head:before{display:none}
    body.arena-visual-system .section-head h2{font-size:clamp(22px,4vw,28px);letter-spacing:0}
    body.arena-visual-system .arena-section-accent:after{
      content:"";
      display:block;
      width:28px;
      height:1px;
      margin-top:8px;
      background:var(--arena-accent);
    }

    body.arena-visual-system :where(.card,.arena-card,.champion-card,.team-card,.stat,.arena-stat,.gi-game,.rank-podium-card,.rank-hall article,.rank-rules article,.league-groups-preview article,.history-values article,.history-timeline article,.history-gallery-card,.registration-card,.registration-admin-card,.auto-standing-card,.champion-ranking-row,.champion-ranking-podium-card){
      border-color:var(--line);
      border-radius:13px;
      background:#0b1710;
      box-shadow:none;
      transition:border-color .15s ease;
    }
    body.arena-visual-system :where(.card,.arena-card,.champion-card,.team-card,.stat,.arena-stat,.gi-game,.rank-podium-card,.league-groups-preview article,.history-values article,.history-gallery-card):hover{
      border-color:rgba(var(--arena-accent-rgb),.24);
      box-shadow:none;
      transform:none;
    }
    body.arena-visual-system .stat{min-height:82px;border-radius:12px}
    body.arena-visual-system .stat:after{display:none}
    body.arena-visual-system :is(.live-card,.match-list,.rank-table-wrap){border-radius:13px}
    body.arena-visual-system :is(.arena-cover,.champion-banner-frame,.history-gallery-media){background:#050a07}

    body.arena-visual-system #arenaScrollProgress{height:1px;background:transparent}
    body.arena-visual-system #arenaScrollProgress i{background:var(--arena-accent);box-shadow:none}
    body.arena-visual-system #arenaBackToTop{
      width:40px;
      height:40px;
      border-color:var(--line);
      border-radius:10px;
      color:var(--arena-accent);
      background:#0b1710;
      box-shadow:none;
    }
    body.arena-visual-system #arenaBackToTop:hover{transform:none}

    #arenaCommandTrigger{
      display:inline-flex;
      align-items:center;
      justify-content:center;
      gap:7px;
      min-height:36px;
      padding:0 10px;
      border:1px solid var(--line);
      border-radius:10px;
      color:#d4ded7;
      background:transparent;
      font-size:11px;
      font-weight:800;
      text-transform:uppercase;
      letter-spacing:.06em;
      white-space:nowrap;
    }
    #arenaCommandTrigger i{color:var(--arena-accent);font-size:13px;font-style:normal}
    #arenaCommandTrigger kbd{padding:2px 4px;border:1px solid var(--line);border-radius:4px;color:#8fa096;background:transparent;font:700 10px/1 Inter,sans-serif}
    #arenaCommandTrigger:hover{border-color:var(--line-strong)}

    .arena-command-backdrop{
      position:fixed;
      inset:0;
      z-index:190;
      display:grid;
      place-items:start center;
      padding:clamp(72px,12vh,120px) 14px 20px;
      background:rgba(0,0,0,.72);
      opacity:0;
      visibility:hidden;
      transition:opacity .14s ease,visibility .14s ease;
    }
    .arena-command-backdrop.show{opacity:1;visibility:visible}
    .arena-command-panel{
      width:min(100%,600px);
      max-height:min(680px,calc(100dvh - 110px));
      overflow:hidden;
      border:1px solid rgba(226,236,229,.14);
      border-radius:14px;
      background:#09140d;
      box-shadow:0 24px 70px rgba(0,0,0,.48);
    }
    .arena-command-head{display:flex;align-items:center;gap:9px;padding:10px;border-bottom:1px solid var(--line)}
    .arena-command-search{position:relative;flex:1}
    .arena-command-search i{position:absolute;left:12px;top:50%;z-index:2;transform:translateY(-50%);color:var(--arena-accent);font-style:normal}
    .arena-command-search input{
      min-height:44px;
      padding:0 12px 0 36px;
      border-color:var(--line);
      border-radius:9px;
      background:#06100a;
      font-size:11px;
      text-transform:none;
      letter-spacing:0;
    }
    .arena-command-close{width:42px;height:42px;border:1px solid var(--line);border-radius:9px;color:#d7e0da;background:transparent;font-size:20px}
    .arena-command-caption{display:flex;justify-content:space-between;gap:12px;padding:11px 13px 6px;color:#82938a;font-size:11px;font-weight:800;letter-spacing:.10em;text-transform:uppercase}
    .arena-command-caption span:last-child{color:var(--arena-accent)}
    .arena-command-results{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:5px;max-height:min(500px,calc(100dvh - 230px));overflow:auto;padding:7px 10px 11px}
    .arena-command-item{
      display:grid;
      grid-template-columns:34px minmax(0,1fr) auto;
      align-items:center;
      gap:9px;
      min-height:56px;
      padding:8px 9px;
      border:1px solid transparent;
      border-radius:9px;
      color:#edf2ee;
      background:transparent;
      text-align:left;
    }
    .arena-command-item:hover,.arena-command-item:focus-visible{border-color:var(--line);background:#0e1d14}
    .arena-command-item>i{display:grid;place-items:center;width:32px;height:32px;border:1px solid var(--line);border-radius:8px;color:var(--arena-accent);font:800 11px/1 Inter,sans-serif;font-style:normal}
    .arena-command-item b,.arena-command-item small{display:block}
    .arena-command-item b{font-size:12px}
    .arena-command-item small{margin-top:3px;color:#82938a;font-size:11px}
    .arena-command-item em{color:#75877d;font-size:17px;font-style:normal}
    .arena-command-empty{grid-column:1/-1;padding:28px;color:#82938a;text-align:center;font-size:12px}
    body.arena-command-open{overflow:hidden}

    /* Interface editorial: menos ornamentos e uma hierarquia de ação previsível. */
    body.arena-visual-system button{
      font-family:Inter,Arial,sans-serif;
      letter-spacing:0;
      text-transform:none;
    }
    body.arena-visual-system .arena-button-ripple{display:none}
    body.arena-visual-system :is(.primary,.secondary,.ghost,.danger){
      min-height:40px;
      padding:0 14px;
      border-radius:6px;
      box-shadow:none;
      font-size:12px;
      font-weight:750;
    }
    body.arena-visual-system .primary{
      color:#15130c;
      border:1px solid #e4ca70;
      background:#d7b856;
    }
    body.arena-visual-system .secondary{
      color:#e1e8e3;
      border:1px solid #2a3b31;
      background:#101c15;
    }
    body.arena-visual-system .ghost{
      color:#d7e0da;
      border:1px solid #26372e;
      background:transparent;
    }
    body.arena-visual-system .danger{
      color:#ff9ba5;
      border:1px solid rgba(255,114,128,.35);
      background:transparent;
    }
    body.arena-visual-system :is(.primary,.secondary,.ghost,.danger):hover{
      transform:none;
      filter:none;
      border-color:var(--arena-accent);
    }
    body.arena-visual-system button:focus-visible{
      outline:2px solid var(--arena-accent);
      outline-offset:2px;
    }
    body.arena-visual-system :is(.icon-btn,.admin-btn,#arenaCommandTrigger,.arena-command-close,.arena-sheet-close){
      border-radius:6px;
      box-shadow:none;
      background:#0c1811;
    }
    body.arena-visual-system .admin-btn.active{
      color:var(--arena-accent);
      border-color:var(--arena-accent);
      background:#101c15;
    }
    body.arena-visual-system [data-page="home"] .hero-actions button{
      min-height:44px;
      padding:0 17px;
      border-radius:6px;
    }
    body.arena-visual-system .home-command{
      gap:13px;
      padding:18px 0;
      border:0;
      border-top:1px solid var(--line);
      border-bottom:1px solid var(--line);
      border-radius:0;
      background:transparent;
      box-shadow:none;
    }
    body.arena-visual-system .home-command h2{
      font-size:clamp(22px,3vw,30px);
      letter-spacing:0;
      text-transform:none;
    }
    body.arena-visual-system .home-online{
      padding:0;
      border:0;
      border-radius:0;
      background:transparent;
    }
    body.arena-visual-system .home-online i{box-shadow:none}
    body.arena-visual-system .home-command nav{gap:7px}
    body.arena-visual-system .home-command nav button{
      grid-template-columns:30px minmax(0,1fr) auto;
      gap:10px;
      min-height:64px;
      padding:10px 11px;
      border:1px solid #223329;
      border-radius:5px;
      color:#e6ece8;
      background:#09130d;
      transition:border-color .14s ease,background .14s ease;
    }
    body.arena-visual-system .home-command nav button:hover{
      transform:none;
      border-color:rgba(var(--arena-accent-rgb),.46);
      background:#0d1b13;
    }
    body.arena-visual-system .home-command nav>button>i{
      width:30px;
      height:30px;
      border:0;
      border-right:1px solid #2a3b31;
      border-radius:0;
      color:var(--arena-accent);
      background:transparent;
      font:800 11px/1 Inter,Arial,sans-serif;
      font-variant-numeric:tabular-nums;
    }
    body.arena-visual-system .home-command nav button b{font-size:12px;font-weight:750}
    body.arena-visual-system .home-command nav button small{margin-top:3px;font-size:11px;font-weight:500}
    body.arena-visual-system .home-command nav button em{color:#718178;font-size:17px}
    body.arena-visual-system :is(.arena-home-card button,.arena-open){
      justify-content:space-between;
      min-height:38px;
      margin-top:13px;
      padding:9px 0;
      border:0;
      border-top:1px solid var(--line);
      border-radius:0;
      color:#d9e2dc;
      background:transparent;
      text-align:left;
    }
    body.arena-visual-system .section-head>button{
      min-height:34px;
      border-radius:5px;
      background:transparent;
    }
    body.arena-visual-system .arena-side-nav .nav-btn{
      grid-template-columns:34px 1fr;
      min-height:49px;
      padding:6px 8px;
      border-radius:6px;
      transition:border-color .14s ease,background .14s ease,color .14s ease;
    }
    body.arena-visual-system .arena-side-nav .nav-btn:hover{
      transform:none;
      background:#0d1a12;
    }
    body.arena-visual-system .arena-side-nav .nav-btn i{
      width:32px;
      height:32px;
      border:0;
      border-radius:4px;
      color:#85968c;
      background:#09140d;
      font:800 11px/1 Inter,Arial,sans-serif;
      font-variant-numeric:tabular-nums;
    }
    body.arena-visual-system .arena-side-nav .nav-btn.active{
      color:#edf2ee;
      border-color:#26382e;
      border-left:2px solid var(--arena-accent);
      background:#101e16;
      box-shadow:none;
    }
    body.arena-visual-system .arena-side-nav .nav-btn.active i{
      color:var(--arena-accent);
      background:#0a150e;
    }
    body.arena-visual-system .arena-side-nav .nav-btn.active .arena-nav-copy small{color:#82938a}
    body.arena-visual-system .arena-side-nav .nav-btn.active:after{display:none}
    body.arena-visual-system .arena-mobile-item{
      border-radius:5px;
      font-weight:700;
    }
    body.arena-visual-system .arena-mobile-item i{
      border-radius:3px;
      font:800 11px/1 Inter,Arial,sans-serif;
    }
    body.arena-visual-system .arena-mobile-item.active{
      color:#edf2ee;
      border-top:2px solid var(--arena-accent);
      background:#101e16;
      box-shadow:none;
    }
    body.arena-visual-system .arena-mobile-item.active i{color:var(--arena-accent);background:transparent}
    body.arena-visual-system .arena-sheet-item{
      border-radius:6px;
      background:#0b1710;
    }
    body.arena-visual-system .arena-sheet-item i{
      border-radius:4px;
      font:800 11px/1 Inter,Arial,sans-serif;
    }

    /* Portal esportivo: informação acima de ornamento. */
    body.arena-visual-system{
      --bg:#060a07;
      --bg-soft:#080e0a;
      --surface:#0b1410;
      --surface-2:#0e1913;
      --surface-3:#111f17;
      --line:rgba(222,232,225,.11);
      --line-strong:rgba(227,196,95,.32);
      background:#060a07;
      color:#edf2ee;
    }
    body.arena-visual-system .topbar{
      min-height:64px;
      border:1px solid var(--line);
      border-radius:9px;
      background:#0a120d;
      box-shadow:none;
      backdrop-filter:none;
    }
    body.arena-visual-system .brand-copy strong{font-size:20px;letter-spacing:.015em}
    body.arena-visual-system .brand-copy span{font-size:11px;letter-spacing:.08em;text-transform:uppercase}
    body.arena-visual-system .brand-mark{border-radius:7px;box-shadow:none}
    body.arena-visual-system .arena-top-status{display:none}
    body.arena-visual-system .cloud-status{
      min-height:32px;
      padding:0 9px;
      border-radius:4px;
      background:transparent;
      font-size:11px;
      letter-spacing:.04em;
      text-transform:none;
    }
    body.arena-visual-system :is(.icon-btn,.admin-btn,#cloudPanelBtn){border-radius:5px;background:#0b1610}
    body.arena-visual-system .arena-clean-panel-icon,
    body.arena-visual-system .arena-clean-admin-icon{
      display:grid;
      place-items:center;
      min-width:20px;
      height:20px;
      border-right:1px solid #2a3b31;
      padding-right:5px;
      font:800 11px/1 Inter,Arial,sans-serif;
      letter-spacing:.04em;
    }
    body.arena-visual-system .arena-notification-button>span{font-size:0;animation:none}
    body.arena-visual-system .arena-notification-button>span:after{display:none;content:none}
    body.arena-visual-system :is(#arenaCommandTrigger,.arena-notification-button,#shareBtn,.arena-clean-panel-icon) svg,
    body.arena-visual-system :is(.hero-actions,.home-command,.arena-command-item,.arena-command-sheet,.arena-command-search) svg{
      display:block;
      width:18px;
      height:18px;
      fill:none;
      stroke:currentColor;
      stroke-width:1.8;
      stroke-linecap:round;
      stroke-linejoin:round;
    }

    @media(min-width:980px){
      body.arena-visual-system .bottom-nav.arena-side-nav{
        width:214px;
        padding:12px;
        border:1px solid var(--line);
        border-radius:10px;
        background:#08110c;
        box-shadow:none;
        backdrop-filter:none;
      }
      body.arena-visual-system .arena-side-brand{padding:4px 4px 14px;border-bottom:1px solid var(--line)}
      body.arena-visual-system .arena-side-logo{border-radius:7px;box-shadow:none}
      body.arena-visual-system .arena-side-heading{padding:10px 5px 2px;color:#788980;font-size:11px;letter-spacing:.12em}
      body.arena-visual-system .arena-side-footer{
        display:block;
        padding:10px 7px;
        border:0;
        border-top:1px solid var(--line);
        border-radius:0;
        background:transparent;
      }
    }

    body.arena-visual-system [data-page="home"] .hero{
      isolation:isolate;
      min-height:330px;
      padding:clamp(25px,4.2vw,48px);
      border:1px solid rgba(227,196,95,.20);
      border-radius:10px;
      background:#0a1810;
      box-shadow:none;
    }
    body.arena-visual-system [data-page="home"] .hero:after{display:none}
    body.arena-visual-system [data-page="home"] .hero:before{display:none}
    body.arena-visual-system [data-page="home"] .hero-content{
      position:relative;
      z-index:2;
      width:min(100%,690px);
      min-height:0;
      padding:0;
    }
    body.arena-visual-system [data-page="home"] .hero h1{
      max-width:650px;
      margin:10px 0 12px;
      font-size:clamp(42px,6vw,64px);
      line-height:.92;
      letter-spacing:-.015em;
      text-transform:uppercase;
    }
    body.arena-visual-system [data-page="home"] .hero p{
      max-width:585px;
      color:#bac7bf;
      font-size:12px;
      line-height:1.62;
    }
    body.arena-visual-system [data-page="home"] .hero-actions{margin-top:21px}
    body.arena-visual-system [data-page="home"] .hero-actions button i{display:grid;place-items:center;font-size:0}
    body.arena-visual-system [data-page="home"] .hero-actions button i svg{width:16px;height:16px}
    body.arena-visual-system .arena-home-watermark{
      position:absolute;
      z-index:1;
      right:clamp(34px,6vw,76px);
      top:50%;
      display:block;
      width:clamp(158px,18vw,224px);
      height:auto;
      opacity:.13;
      pointer-events:none;
      user-select:none;
      filter:saturate(.72) contrast(1.05);
      transform:translateY(-50%);
      animation:arenaWatermarkFloat 9s ease-in-out infinite;
    }
    @keyframes arenaWatermarkFloat{
      0%,100%{transform:translateY(-50%) scale(1)}
      50%{transform:translateY(calc(-50% - 6px)) scale(1.025)}
    }
    body.arena-visual-system .arena-hero-layout{display:block;padding:0}
    body.arena-visual-system .arena-hero-console{display:none}

    body.arena-visual-system .eyebrow{
      display:inline-flex;
      min-height:0;
      padding:0;
      border:0;
      border-radius:0;
      color:#cdb45e;
      background:transparent;
      font-size:11px;
      line-height:1.2;
      letter-spacing:.12em;
      text-transform:uppercase;
    }
    body.arena-visual-system .eyebrow:before{display:none}
    body.arena-visual-system .section-head{
      margin:28px 0 12px;
      padding:0;
      border-left:0;
    }
    body.arena-visual-system .section-head h2{
      margin-top:4px;
      font-size:clamp(23px,3vw,29px);
      line-height:1.05;
      letter-spacing:0;
      text-transform:none;
    }
    body.arena-visual-system .section-head p{color:#84958b;font-size:12px}
    body.arena-visual-system .arena-section-accent:after{display:none}

    body.arena-visual-system #arenaStats{
      display:grid;
      grid-template-columns:repeat(5,minmax(0,1fr));
      gap:0;
      margin:10px 0 30px;
      overflow:hidden;
      border:1px solid var(--line);
      border-radius:8px;
      background:#09130d;
    }
    body.arena-visual-system #arenaStats .stat{
      min-height:72px;
      padding:13px 15px;
      border:0;
      border-right:1px solid var(--line);
      border-radius:0;
      background:transparent;
      box-shadow:none;
    }
    body.arena-visual-system #arenaStats .stat:last-child{border-right:0}
    body.arena-visual-system #arenaStats .stat b{color:#e3c45f;font-size:25px;font-variant-numeric:tabular-nums}
    body.arena-visual-system #arenaStats .stat span{margin-top:6px;color:#84958b;font-size:11px;letter-spacing:.07em}

    body.arena-visual-system .home-command{margin:0;padding:17px 0 20px}
    body.arena-visual-system .home-command>header{display:block}
    body.arena-visual-system .home-command h2{margin:4px 0 0;font-size:25px}
    body.arena-visual-system .home-online{display:none}
    body.arena-visual-system .home-command nav button{
      grid-template-columns:34px minmax(0,1fr) 16px;
      min-height:66px;
      padding:10px;
      border-color:#223329;
      background:#09120d;
    }
    body.arena-visual-system .home-command nav button:hover{border-color:#5a5132;background:#0c1811}
    body.arena-visual-system .home-command nav>button>i{
      width:34px;
      height:34px;
      border-right:1px solid #2a3b31;
      color:#d7b856;
      font-size:0;
    }
    body.arena-visual-system .home-command nav>button>i svg{width:18px;height:18px}
    body.arena-visual-system .home-command nav button em{display:grid;place-items:center;font-size:0}
    body.arena-visual-system .home-command nav button em svg{width:14px;height:14px}
    body.arena-visual-system .home-command nav button b{font-weight:700}
    body.arena-visual-system .arena-home-scroll{gap:8px}
    body.arena-visual-system .arena-home-card{
      min-height:158px;
      padding:15px;
      border-radius:7px;
      background:#0a1510;
      box-shadow:none;
    }
    body.arena-visual-system .arena-home-card:before{display:none;content:none}
    body.arena-visual-system .arena-home-card h3{margin:9px 0 6px;font-size:24px;letter-spacing:0}

    body.arena-visual-system :where(.card,.form-card,.admin-panel,.arena-card,.champion-card,.team-card,.arena-stat,.gi-game,.rank-podium-card,.rank-hall article,.rank-rules article,.league-groups-preview article,.history-values article,.history-timeline article,.history-gallery-card,.registration-card,.registration-admin-card,.auto-standing-card,.champion-ranking-row,.champion-ranking-podium-card){
      border-color:var(--line);
      border-radius:8px;
      background:#0b1510;
      box-shadow:none;
    }
    body.arena-visual-system :where(.card,.arena-card,.champion-card,.team-card,.arena-stat,.gi-game):hover{transform:none;box-shadow:none}
    body.arena-visual-system :is(.arena-symbol,.arena-hero-symbol){display:none}
    body.arena-visual-system .champion-card:after{display:none}
    body.arena-visual-system :is(.arena-status,.arena-filter,.match-status,.live-pill){
      border-radius:4px;
      box-shadow:none;
      text-shadow:none;
    }
    body.arena-visual-system .arena-filter.active{background:#d7b856}
    body.arena-visual-system :is(input,select,textarea){border-radius:6px;background:#07100b;box-shadow:none}
    body.arena-visual-system :is(.modal,.arena-notification-panel,.arena-notification-editor,.arena-nav-sheet){border-radius:10px;background:#0b1510;box-shadow:none;backdrop-filter:none}
    body.arena-visual-system .admin-panel{background:#0b1510}
    body.arena-visual-system .admin-panel h3{text-transform:none;letter-spacing:0}
    body.arena-visual-system #arenaBackToTop{border-radius:5px}
    body.arena-visual-system .arena-page-back{
      min-width:80px;
      height:40px;
      padding:0 12px;
      border:1px solid #2a3a31;
      border-radius:6px;
      color:#dce5df;
      background:#0a1610;
      box-shadow:none;
      font-size:12px;
      font-weight:700;
    }
    body.arena-visual-system .arena-page-back:hover{border-color:#5b5130;background:#0d1b13}

    /* Estrutura de aplicativo esportivo: conteúdo primeiro, ações previsíveis e placares legíveis. */
    body.arena-visual-system [data-page="home"]{gap:14px}
    body.arena-visual-system [data-page="home"] .hero{
      min-height:226px;
      padding:30px 34px;
      border-color:#294735;
      border-radius:12px;
      background:#10291a;
    }
    body.arena-visual-system [data-page="home"] .hero-content{width:min(100%,610px)}
    body.arena-visual-system [data-page="home"] .hero h1{
      max-width:560px;
      margin:8px 0 9px;
      font-size:clamp(40px,5vw,54px);
      line-height:.96;
    }
    body.arena-visual-system [data-page="home"] .hero p{max-width:525px;font-size:11px}
    body.arena-visual-system [data-page="home"] .hero-actions{margin-top:17px}
    body.arena-visual-system [data-page="home"] .hero-actions button{min-height:42px}
    body.arena-visual-system .arena-home-watermark{right:58px;width:188px;opacity:.11}

    body.arena-visual-system .arena-app-tabs{
      display:flex;
      align-items:center;
      gap:2px;
      overflow-x:auto;
      min-height:54px;
      padding:0 8px;
      border:1px solid #20372a;
      border-radius:10px;
      background:#0a1710;
      scrollbar-width:none;
    }
    body.arena-visual-system .arena-app-tabs::-webkit-scrollbar{display:none}
    body.arena-visual-system .arena-app-tab{
      position:relative;
      display:flex;
      align-items:center;
      justify-content:center;
      flex:1 0 auto;
      gap:7px;
      min-width:104px;
      min-height:52px;
      padding:0 13px;
      border:0;
      border-radius:0;
      color:#8ea097;
      background:transparent;
      font-size:12px;
      font-weight:700;
      white-space:nowrap;
    }
    body.arena-visual-system .arena-app-tab:hover{color:#e1e9e4;background:#0d1d14}
    body.arena-visual-system .arena-app-tab.active{color:#ead277;background:transparent}
    body.arena-visual-system .arena-app-tab.active:after{
      content:"";
      position:absolute;
      right:13px;
      bottom:0;
      left:13px;
      height:2px;
      background:#d7b856;
    }
    body.arena-visual-system .arena-app-tab svg{
      display:block;
      width:17px;
      height:17px;
      fill:none;
      stroke:currentColor;
      stroke-width:1.8;
      stroke-linecap:round;
      stroke-linejoin:round;
    }

    body.arena-visual-system #arenaStats{
      margin:0 0 6px;
      border-color:#20372a;
      border-radius:10px;
      background:#0a1710;
    }
    body.arena-visual-system #arenaStats .stat{min-height:64px;padding:11px 14px}
    body.arena-visual-system #arenaStats .stat b{font-size:22px}
    body.arena-visual-system #arenaStats .stat span{margin-top:4px;font-size:11px}

    body.arena-visual-system .section-head{margin:20px 0 10px}
    body.arena-visual-system .section-head h2{font-size:24px}
    body.arena-visual-system .home-tournaments{gap:8px}
    body.arena-visual-system .arena-home-scroll{
      grid-auto-flow:row;
      grid-auto-columns:auto;
      grid-template-columns:repeat(2,minmax(0,1fr));
      gap:8px;
      overflow:visible;
      padding:0;
    }
    body.arena-visual-system .arena-home-card{
      display:grid;
      grid-template-columns:minmax(0,1fr) auto;
      grid-template-rows:auto auto auto;
      align-content:center;
      align-items:center;
      min-height:112px;
      padding:14px 15px;
      border-color:#21392b;
      border-left:3px solid #d7b856;
      border-radius:9px;
      background:#0b1911;
    }
    body.arena-visual-system .arena-home-card .eyebrow,
    body.arena-visual-system .arena-home-card h3,
    body.arena-visual-system .arena-home-card p{grid-column:1;min-width:0}
    body.arena-visual-system .arena-home-card h3{
      overflow:hidden;
      margin:5px 12px 3px 0;
      font-size:21px;
      line-height:1;
      text-overflow:ellipsis;
      white-space:nowrap;
    }
    body.arena-visual-system .arena-home-card button{
      grid-column:2;
      grid-row:1/4;
      align-self:center;
      min-height:36px;
      margin:0;
      padding:0 11px;
      border:1px solid #304636;
      border-radius:6px;
      background:#102219;
      white-space:nowrap;
    }

    body.arena-visual-system .home-command{
      margin-top:8px;
      padding:15px;
      border:1px solid #20372a;
      border-radius:10px;
      background:#08140d;
    }
    body.arena-visual-system .home-command h2{font-size:22px}
    body.arena-visual-system .home-command nav{grid-template-columns:repeat(5,minmax(0,1fr))}
    body.arena-visual-system .home-command nav button{
      grid-template-columns:30px minmax(0,1fr) 13px;
      min-height:58px;
      padding:8px;
      border-color:#20372a;
      border-radius:7px;
      background:#0b1911;
    }
    body.arena-visual-system .home-command nav>button>i{width:30px;height:30px}
    body.arena-visual-system .home-command nav>button>i svg{width:16px;height:16px}

    body.arena-visual-system [data-page="home"] .home-grid{gap:10px}
    body.arena-visual-system :is(.now-feature,.now-list){
      border-color:#21392b;
      border-radius:10px;
      background:#0b1911;
      box-shadow:none;
    }
    body.arena-visual-system .now-feature>header{background:#0e2117}
    body.arena-visual-system .now-pill{border-radius:4px}
    body.arena-visual-system .now-feature>main{min-height:166px}
    body.arena-visual-system .now-row{min-height:68px}
    body.arena-visual-system .now-state{
      padding:5px 7px;
      border-radius:4px;
      background:#102219;
      text-transform:uppercase;
    }

    body.arena-visual-system .arena-page-hero{
      min-height:220px;
      padding:28px 30px;
      border-color:#294735;
      border-radius:12px;
      background:#10291a;
      box-shadow:none;
    }
    body.arena-visual-system .arena-page-hero:after{opacity:.55}
    body.arena-visual-system .arena-page-hero h1{
      margin:7px 0 8px;
      font-size:clamp(44px,7vw,68px);
      line-height:.86;
    }
    body.arena-visual-system .arena-page-hero p{font-size:11px}
    body.arena-visual-system .arena-page-hero aside>b{font-size:52px}
    body.arena-visual-system .arena-catalog-head{margin:19px 0 10px}
    body.arena-visual-system .arena-catalog-head h2{font-size:27px;text-transform:none}
    body.arena-visual-system .arena-catalog-head .arena-toolbar{
      padding:4px;
      border-radius:8px;
      background:#0a1710;
    }
    body.arena-visual-system .arena-detail-nav{
      margin:14px 0 10px;
      padding:6px 7px 6px 12px;
      border-color:#20372a;
      border-radius:9px;
      background:#0a1710;
      box-shadow:none;
    }
    body.arena-visual-system .arena-detail-nav nav{gap:1px}
    body.arena-visual-system .arena-detail-nav button{
      min-height:36px;
      border-radius:5px;
      text-transform:none;
    }
    body.arena-visual-system .arena-grid{gap:9px}
    body.arena-visual-system .arena-card{
      min-height:300px;
      border-color:#21392b;
      border-radius:10px;
      background:#0b1911;
    }
    body.arena-visual-system .arena-card .arena-cover{height:132px}
    body.arena-visual-system .arena-card .arena-body{min-height:166px;padding:14px}
    body.arena-visual-system .arena-card .arena-body h3{font-size:23px}
    body.arena-visual-system .arena-card .arena-open{min-height:36px}

    @media(max-width:720px){
      body.arena-visual-system{
        overflow-x:clip;
        padding-bottom:calc(92px + env(safe-area-inset-bottom));
        scroll-padding-bottom:calc(84px + env(safe-area-inset-bottom));
      }
      body.arena-visual-system main{padding-bottom:calc(82px + env(safe-area-inset-bottom))}
      body.arena-visual-system .topbar{
        top:0;
        min-height:57px;
        margin:0 0 12px;
        padding:8px 12px;
        gap:6px;
        border-width:0 0 1px;
        border-color:#223128;
        border-radius:0;
        background:rgba(7,16,11,.98);
        box-shadow:0 8px 24px rgba(0,0,0,.18);
        backdrop-filter:none;
      }
      body.arena-visual-system.arena-has-page-back .topbar .brand{display:none}
      body.arena-visual-system .topbar .brand{
        width:auto;
        min-width:40px;
        height:40px;
        flex:0 0 auto;
        gap:0;
      }
      body.arena-visual-system .topbar .brand-mark{width:40px;min-width:40px;height:40px;border-radius:7px}
      body.arena-visual-system .topbar .brand-copy{display:none}
      body.arena-visual-system .topbar .top-actions{
        min-width:0;
        flex:1 1 auto;
        justify-content:flex-end;
        gap:4px;
      }
      body.arena-visual-system .arena-page-back{
        width:auto;
        min-width:76px;
        height:40px;
        padding:0 11px;
        gap:7px;
        border-radius:6px;
      }
      body.arena-visual-system .arena-page-back span{position:static;width:auto;height:auto;overflow:visible;clip-path:none;white-space:nowrap}
      body.arena-visual-system .arena-page-back svg{width:17px;height:17px}
      body.arena-visual-system .topbar :is(#arenaNotificationsBtn,#shareBtn){width:40px;min-width:40px;height:40px;min-height:40px;padding:0;border-radius:6px;background:#0a1610}
      body.arena-visual-system .topbar #cloudPanelBtn{
        width:40px;
        min-width:40px;
        height:40px;
        min-height:40px;
        padding:0;
        border-radius:6px;
        color:#dce5df;
        background:#0a1610;
      }
      body.arena-visual-system .topbar #cloudPanelBtn span{display:none}
      body.arena-visual-system .topbar #cloudPanelBtn svg{width:18px;height:18px}
      body.arena-visual-system .topbar #adminBtn{
        width:52px;
        min-width:52px;
        height:40px;
        min-height:40px;
        padding:0 6px;
        overflow:hidden;
        border-radius:6px;
        background:#0a1610;
        font-size:0;
        white-space:nowrap;
      }
      body.arena-visual-system .topbar #adminBtn:before{content:"Entrar";font-size:11px}
      body.arena-visual-system .topbar #adminBtn[data-account-state="member"]:before{content:"Conta"}
      body.arena-visual-system .topbar #adminBtn[data-account-state="admin"]:before{content:"Admin"}
      body.arena-visual-system .topbar #memberLogoutBtn{
        width:48px;
        min-width:48px;
        height:40px;
        min-height:40px;
        padding:0 5px;
        border-radius:6px;
        font-size:11px;
        white-space:nowrap;
      }
      body.arena-visual-system [data-page="home"] .hero{min-height:310px;padding:27px 20px;border-radius:8px}
      body.arena-visual-system [data-page="home"] .hero h1{font-size:clamp(42px,13vw,58px)}
      body.arena-visual-system .arena-home-watermark{right:-24px;top:35%;width:142px;opacity:.065}
      body.arena-visual-system .arena-app-tabs{
        display:grid;
        grid-template-columns:repeat(5,minmax(0,1fr));
        gap:0;
        overflow:visible;
        padding:0;
      }
      body.arena-visual-system .arena-app-tab{
        min-width:0;
        min-height:58px;
        padding:5px 2px;
        flex-direction:column;
        gap:4px;
        font-size:11px;
      }
      body.arena-visual-system .arena-app-tab.active:after{right:5px;left:5px}
      body.arena-visual-system #arenaStats{
        grid-template-columns:repeat(2,minmax(0,1fr));
        gap:1px;
        overflow:visible;
        border:0;
        background:#20372a;
      }
      body.arena-visual-system #arenaStats .stat{
        min-width:0;
        min-height:72px;
        padding:12px 14px;
        border:0;
        background:#0a1710;
      }
      body.arena-visual-system #arenaStats .stat:last-child{grid-column:1/-1}
      body.arena-visual-system #arenaStats .stat span{
        line-height:1.25;
        letter-spacing:.04em;
        overflow-wrap:anywhere;
      }
      body.arena-visual-system .admin-panel.show{scroll-margin-bottom:calc(84px + env(safe-area-inset-bottom))}
      body.arena-visual-system .admin-panel.show .admin-tools{display:grid;grid-template-columns:1fr}
      body.arena-visual-system .admin-panel.show .admin-tools button{width:100%;min-width:0}
      body.arena-visual-system .arena-mobile-nav{
        left:0;
        right:0;
        bottom:0;
        width:100%;
        height:calc(64px + env(safe-area-inset-bottom));
        padding:4px 8px env(safe-area-inset-bottom);
        gap:2px;
        border-width:1px 0 0;
        border-color:#223128;
        border-radius:0;
        background:rgba(6,14,10,.985);
        box-shadow:0 -8px 24px rgba(0,0,0,.24);
        backdrop-filter:none;
      }
      body.arena-visual-system .arena-mobile-item{
        position:relative;
        min-height:56px;
        gap:3px;
        border-radius:5px;
        color:#7f9187;
        background:transparent;
        box-shadow:none;
        font-size:10.5px;
        font-weight:650;
      }
      body.arena-visual-system .arena-mobile-item span{letter-spacing:-.02em}
      body.arena-visual-system .arena-mobile-item i{width:24px;height:24px;color:inherit;background:transparent}
      body.arena-visual-system .arena-mobile-item i svg{width:19px;height:19px;stroke-width:1.75}
      body.arena-visual-system .arena-mobile-item.active{color:#e3c45f;background:transparent}
      body.arena-visual-system .arena-mobile-item.active:before{content:"";position:absolute;left:24%;right:24%;top:-4px;height:2px;border-radius:0 0 2px 2px;background:#d7b856}
      body.arena-visual-system .arena-nav-sheet{padding:16px 14px max(14px,env(safe-area-inset-bottom));border-radius:12px 12px 0 0;background:#08130d;box-shadow:0 -18px 48px rgba(0,0,0,.42)}
      body.arena-visual-system .arena-nav-sheet h2{font-size:24px;letter-spacing:0;text-transform:none}
      body.arena-visual-system .arena-sheet-item{grid-template-columns:38px 1fr auto;min-height:58px;padding:8px 10px;border-radius:6px;background:transparent}
      body.arena-visual-system .arena-sheet-item i{width:36px;height:36px;border-radius:6px;background:#0b1911}
      body.arena-visual-system .arena-sheet-item i svg{width:18px;height:18px}
      body.arena-visual-system .arena-home-scroll{grid-auto-columns:82%}
      body.arena-visual-system [data-page="home"] .hero{min-height:238px;padding:24px 19px}
      body.arena-visual-system [data-page="home"] .hero h1{font-size:clamp(38px,12vw,50px)}
      body.arena-visual-system .arena-home-watermark{right:-25px;top:38%;width:132px;opacity:.055}
      body.arena-visual-system .arena-app-tabs{margin-inline:-1px;padding-inline:2px;border-radius:8px}
      body.arena-visual-system .arena-app-tab{min-width:0;padding-inline:2px}
      body.arena-visual-system .arena-home-scroll{grid-template-columns:1fr}
      body.arena-visual-system .arena-home-card{min-height:102px}
      body.arena-visual-system .home-command{padding:12px}
      body.arena-visual-system .home-command nav{grid-template-columns:1fr 1fr}
      body.arena-visual-system .home-command nav button:last-child{grid-column:1/-1}
      body.arena-visual-system .arena-page-hero{min-height:230px;padding:23px 18px;border-radius:9px}
      body.arena-visual-system .arena-page-hero h1{font-size:48px}
      body.arena-visual-system .arena-detail-nav{top:58px;border-radius:8px}
      body.arena-visual-system .arena-card{min-height:286px}
    }

    @media(max-width:360px){
      body.arena-visual-system .topbar{padding-inline:8px}
      body.arena-visual-system .arena-page-back{min-width:70px;padding-inline:8px}
      body.arena-visual-system .topbar #shareBtn{display:none}
    }

    @media(max-width:900px){body.arena-visual-system .home-command nav{grid-template-columns:repeat(2,minmax(0,1fr))}}
    @media(max-width:520px){body.arena-visual-system .home-command nav{grid-template-columns:1fr}}
    @media(max-width:520px){
      body.arena-visual-system .home-command nav{grid-template-columns:1fr}
      body.arena-visual-system .home-command nav button:last-child{grid-column:auto}
    }

    @media(max-width:1180px){#arenaCommandTrigger span{display:none}}
    @media(max-width:720px){
      #arenaCommandTrigger{display:none}
      .arena-command-backdrop{align-items:end;padding:10px 10px max(10px,env(safe-area-inset-bottom))}
      .arena-command-panel{max-height:calc(100dvh - 20px);border-radius:13px}
      .arena-command-results{grid-template-columns:1fr;max-height:calc(100dvh - 168px)}
      .arena-command-caption span:first-child{display:none}
      body.arena-visual-system .hero{min-height:260px;border-radius:15px}
    }
    @media(prefers-reduced-motion:reduce){.arena-command-backdrop{transition:none}.arena-home-watermark{animation:none}}
  `;
  document.head.append(style);

  function go(page) {
    if (!PAGES[page]) return;
    if (typeof navigate === 'function') navigate(page);
    else {
      const target = $(`[data-go="${page}"]`) || $(`[data-mobile-go="${page}"]`) || $(`[data-sheet-go="${page}"]`);
      if (target) target.click();
      else $$('.page').forEach(section => section.classList.toggle('active', section.dataset.page === page));
    }
    syncPage();
  }

  function currentPage() {
    return $('.page.active')?.dataset.page || 'home';
  }

  function installHeroWatermark() {
    const hero = $('[data-page="home"] .hero');
    if (!hero || $('.arena-home-watermark', hero)) return;
    const watermark = document.createElement('img');
    watermark.className = 'arena-home-watermark';
    watermark.src = brandIconUrl();
    watermark.alt = '';
    watermark.decoding = 'async';
    watermark.setAttribute('aria-hidden', 'true');
    hero.append(watermark);
  }

  function enhanceHomeShortcuts() {
    const command = $('.home-command');
    if (!command) return;
    const heading = $('h2', command);
    if (heading && heading.textContent !== 'Acesso rápido') heading.textContent = 'Acesso rápido';
    $$('nav button[data-go]', command).forEach(button => {
      const icon = $(':scope > i', button);
      const arrow = $(':scope > em', button);
      if (icon && !$('svg', icon)) icon.innerHTML = interfaceIcon(button.dataset.go);
      if (arrow && !$('svg', arrow)) arrow.innerHTML = interfaceIcon('arrowRight');
    });
  }

  function enhanceInterfaceIcons() {
    const replacements = [
      ['#arenaCommandTrigger > i', 'search'],
      ['#arenaCommandPalette .arena-command-search > i', 'search'],
      ['#arenaNotificationsBtn > span', 'notifications'],
      ['#shareBtn', 'share'],
      ['.arena-clean-panel-icon', 'settings'],
      ['[data-page="home"] .hero-actions .primary i', 'arrowRight'],
      ['[data-page="home"] .hero-actions .secondary i', 'share']
    ];
    replacements.forEach(([selector, name]) => {
      const target = $(selector);
      if (target && !$('svg', target)) target.innerHTML = interfaceIcon(name);
    });
  }

  function installCompetitionAppHome() {
    const home = $('[data-page="home"]');
    if (!home) return;
    const hero = $('.hero', home);
    const stats = $('#arenaStats', home);
    let tabs = $('.arena-app-tabs', home);
    if (!tabs) {
      tabs = document.createElement('nav');
      tabs.className = 'arena-app-tabs';
      tabs.setAttribute('aria-label', 'Atalhos da competição');
      tabs.innerHTML = [
        ['home', 'home', 'Visão geral'],
        ['tournament', 'tournament', 'Jogos'],
        ['season', 'season', 'Tabela'],
        ['teams', 'teams', 'Times'],
        ['news', 'news', 'Notícias']
      ].map(([page, icon, label], index) => `<button type="button" class="arena-app-tab${index === 0 ? ' active' : ''}" data-go="${page}">${interfaceIcon(icon)}<span>${label}</span></button>`).join('');
    }
    if (stats && tabs.nextElementSibling !== stats) home.insertBefore(tabs, stats);
    else if (!stats && hero?.nextElementSibling !== tabs) hero?.after(tabs);

    const tournaments = $('.home-tournaments', home);
    const command = $('.home-command', home);
    if (tournaments) {
      const heading = $('.section-head h2', tournaments);
      const description = $('.section-head p', tournaments);
      if (heading && heading.textContent !== 'Seus campeonatos') heading.textContent = 'Seus campeonatos';
      if (description && description.textContent !== 'Acompanhe status, edições e resultados.') description.textContent = 'Acompanhe status, edições e resultados.';
    }
    if (tournaments && command && tournaments.nextElementSibling !== command) tournaments.after(command);
  }

  function renderCommandResults(container, query = '') {
    const normalize = value => value.toLocaleLowerCase('pt-BR').normalize('NFD').replace(/[\u0300-\u036f]/g, '');
    const normalized = normalize(query.trim());
    const entries = Object.entries(PAGES).filter(([, meta]) => !normalized || normalize(`${meta.label} ${meta.description}`).includes(normalized));
    container.innerHTML = entries.length ? entries.map(([page, meta]) => `
      <button class="arena-command-item" type="button" data-command-page="${page}">
        <i aria-hidden="true">${interfaceIcon(page)}</i>
        <span><b>${meta.label}</b><small>${meta.description}</small></span>
        <em aria-hidden="true">${interfaceIcon('arrowRight')}</em>
      </button>`).join('') : '<div class="arena-command-empty">Nenhuma área encontrada.</div>';
  }

  function installCommandPalette() {
    if ($('#arenaCommandPalette')) return;
    const topActions = $('.top-actions');
    const trigger = document.createElement('button');
    trigger.id = 'arenaCommandTrigger';
    trigger.type = 'button';
    trigger.setAttribute('aria-label', 'Abrir acesso rápido');
    trigger.innerHTML = '<i aria-hidden="true">⌕</i><span>Buscar</span><kbd>Ctrl K</kbd>';
    topActions?.prepend(trigger);

    const backdrop = document.createElement('div');
    backdrop.id = 'arenaCommandPalette';
    backdrop.className = 'arena-command-backdrop';
    backdrop.setAttribute('aria-hidden', 'true');
    backdrop.innerHTML = `
      <section class="arena-command-panel" role="dialog" aria-modal="true" aria-labelledby="arenaCommandTitle">
        <header class="arena-command-head">
          <div class="arena-command-search"><i aria-hidden="true">⌕</i><input id="arenaCommandInput" type="search" autocomplete="off" placeholder="Buscar área" aria-label="Buscar área da Arena BDA"></div>
          <button class="arena-command-close" type="button" aria-label="Fechar">×</button>
        </header>
        <div class="arena-command-caption"><span id="arenaCommandTitle">Navegação</span><span>11 áreas</span></div>
        <div class="arena-command-results"></div>
      </section>`;
    document.body.append(backdrop);

    const input = $('#arenaCommandInput', backdrop);
    const results = $('.arena-command-results', backdrop);
    let previousFocus = null;

    const open = () => {
      previousFocus = document.activeElement;
      renderCommandResults(results);
      input.value = '';
      backdrop.classList.add('show');
      backdrop.setAttribute('aria-hidden', 'false');
      document.body.classList.add('arena-command-open');
      setTimeout(() => input.focus(), 30);
    };
    const close = () => {
      backdrop.classList.remove('show');
      backdrop.setAttribute('aria-hidden', 'true');
      document.body.classList.remove('arena-command-open');
      previousFocus?.focus?.();
    };

    trigger.addEventListener('click', open);
    $('.arena-command-close', backdrop).addEventListener('click', close);
    input.addEventListener('input', () => renderCommandResults(results, input.value));
    results.addEventListener('click', event => {
      const button = event.target.closest('[data-command-page]');
      if (!button) return;
      go(button.dataset.commandPage);
      close();
    });
    backdrop.addEventListener('click', event => event.target === backdrop && close());
    document.addEventListener('keydown', event => {
      const shortcut = (event.ctrlKey || event.metaKey) && event.key.toLocaleLowerCase() === 'k';
      if (shortcut) {
        event.preventDefault();
        backdrop.classList.contains('show') ? close() : open();
      } else if (event.key === 'Escape' && backdrop.classList.contains('show')) close();
    });

    window.ArenaBDAQuickAccess = Object.freeze({ open, close });
  }

  function installMobileCommandShortcut() {
    const grid = $('.arena-sheet-grid');
    if (!grid || $('.arena-command-sheet', grid)) return;
    const button = document.createElement('button');
    button.type = 'button';
    button.className = 'arena-sheet-item arena-command-sheet';
    button.innerHTML = `<i aria-hidden="true">${interfaceIcon('search')}</i><span><b>Buscar</b><small>Acessar qualquer área</small></span><em>${interfaceIcon('arrowRight')}</em>`;
    button.addEventListener('click', () => {
      $('.arena-nav-sheet-backdrop')?.classList.remove('show');
      document.body.classList.remove('arena-sheet-open');
      window.ArenaBDAQuickAccess?.open();
    });
    grid.prepend(button);
  }

  function enhanceSectionHeads(root = document) {
    $$('.section-head h2', root).forEach(heading => heading.classList.add('arena-section-accent'));
  }

  function syncPage() {
    const page = currentPage();
    const meta = PAGES[page] || PAGES.home;
    document.body.dataset.visualPage = page;
    document.body.style.setProperty('--arena-accent', meta.color);
    document.body.style.setProperty('--arena-accent-rgb', meta.rgb);
    enhanceSectionHeads($(`.page[data-page="${page}"]`) || document);
  }

  function refresh() {
    installCommandPalette();
    installMobileCommandShortcut();
    installHeroWatermark();
    enhanceHomeShortcuts();
    enhanceInterfaceIcons();
    installCompetitionAppHome();
    enhanceSectionHeads();
    syncPage();
  }

  const observerRoot = $('.app-shell') || document.body;
  const pageObserver = new MutationObserver(syncPage);
  const observePage = page => pageObserver.observe(page, {
    attributes: true,
    attributeFilter: ['class']
  });
  $$('.page', observerRoot).forEach(observePage);

  const STRUCTURE_SELECTOR = '.page,.section-head,.arena-sheet-grid,.hero,.home-command,.home-tournaments,.top-actions,#arenaStats,#arenaNotificationsBtn';
  let refreshFrame = 0;
  const structureObserver = new MutationObserver(mutations => {
    const addedElements = mutations.flatMap(mutation => [...mutation.addedNodes]).filter(node => node instanceof Element);
    if (!addedElements.some(node => node.matches(STRUCTURE_SELECTOR) || node.querySelector(STRUCTURE_SELECTOR))) return;
    if (refreshFrame) return;
    refreshFrame = requestAnimationFrame(() => {
      refreshFrame = 0;
      addedElements.forEach(node => {
        if (node.matches('.page')) observePage(node);
        node.querySelectorAll?.('.page').forEach(observePage);
      });
      refresh();
    });
  });
  structureObserver.observe(observerRoot, { childList: true, subtree: true });

  document.addEventListener('click', event => {
    if (event.target.closest('[data-go],[data-mobile-go],[data-sheet-go]')) setTimeout(syncPage, 30);
  });

  [0, 700].forEach(delay => setTimeout(refresh, delay));
})();

;
