/* Generated by scripts/build-production-bundles.sh. Edit the source modules, then rebuild. */

/* ===== Source: navegacao-arena-v2.js ===== */
(() => {
  'use strict';

  const PRIMARY_PAGES = ['home', 'tournament', 'registrations', 'champions', 'teams', 'community'];
  const SECONDARY_PAGES = ['news', 'history', 'flash', 'season', 'feedback'];
  const MOBILE_PRIMARY_PAGES = ['home', 'tournament', 'registrations', 'champions'];
  const MOBILE_SECONDARY_PAGES = ['news', 'history', 'flash', 'season', 'teams', 'community', 'feedback'];
  const ORDER = [...PRIMARY_PAGES, ...SECONDARY_PAGES];
  const HISTORY_PAGE_KEY = 'arenaNavigationPage';
  const HISTORY_DEPTH_KEY = 'arenaNavigationDepth';
  const META = {
    home: ['01', 'Início', 'Visão geral da arena'],
    tournament: ['02', 'Campeonatos', 'Copas, ligas e confrontos'],
    registrations: ['03', 'Inscrições', 'Vagas e aprovações'],
    champions: ['04', 'Campeões', 'Sala de troféus'],
    teams: ['05', 'Times', 'Clubes cadastrados'],
    community: ['06', 'Comunidade', 'Arquibancada do clã'],
    news: ['07', 'Notícias', 'Comunicados e novidades'],
    history: ['08', 'História', 'Memória oficial do clã'],
    flash: ['09', 'Copas Flash', 'Edições rápidas'],
    season: ['10', 'Temporada', 'Calendário e classificação'],
    feedback: ['11', 'Feedback', 'Ajude a melhorar a Arena']
  };

  const $ = (selector, root = document) => root.querySelector(selector);
  const $$ = (selector, root = document) => [...root.querySelectorAll(selector)];
  const iconUrl = () => $('link[rel~="icon"]')?.href || './favicon.svg';
  const currentPage = () => $('.page.active')?.dataset.page || 'home';

  let mobileNav;
  let sheet;
  let pageBackButton;
  let viewportFrame = 0;

  function normalizePage(page) {
    return ORDER.includes(page) && $(`.page[data-page="${page}"]`) ? page : 'home';
  }

  function historyDepth() {
    const value = Number(window.history.state?.[HISTORY_DEPTH_KEY]);
    return Number.isInteger(value) && value > 0 ? value : 0;
  }

  function routeUrl(page) {
    const url = new URL(window.location.href);
    url.hash = page === 'home' ? '' : page;
    return `${url.pathname}${url.search}${url.hash}`;
  }

  function routeState(page, depth) {
    const currentState = window.history.state;
    return {
      ...(currentState && typeof currentState === 'object' ? currentState : {}),
      [HISTORY_PAGE_KEY]: page,
      [HISTORY_DEPTH_KEY]: depth
    };
  }

  function syncPageBack() {
    if (!pageBackButton) return;
    const visible = historyDepth() > 0 || currentPage() !== 'home';
    pageBackButton.hidden = !visible;
  }

  function installHistoryNavigation() {
    if (window.navigate?.arenaHistoryEnabled) return;

    const renderPage = typeof window.navigate === 'function'
      ? window.navigate.bind(window)
      : page => {
          $$('.page').forEach(item => item.classList.toggle('active', item.dataset.page === page));
          window.scrollTo({ top: 0, behavior: 'smooth' });
        };
    const requestedPage = normalizePage(window.location.hash.slice(1));

    const navigateWithHistory = (page, options = {}) => {
      const targetPage = normalizePage(page);
      const activePage = currentPage();
      const fromHistory = options?.fromHistory === true;

      if (!fromHistory && targetPage !== activePage) {
        const depth = historyDepth();
        if (options?.replace === true) {
          window.history.replaceState(routeState(targetPage, depth), '', routeUrl(targetPage));
        } else {
          window.history.pushState(routeState(targetPage, depth + 1), '', routeUrl(targetPage));
        }
      }

      renderPage(targetPage);
      closeSheet();
      requestAnimationFrame(() => {
        syncActive();
        syncPageBack();
      });
    };

    navigateWithHistory.arenaHistoryEnabled = true;
    window.navigate = navigateWithHistory;

    window.history.replaceState(routeState('home', 0), '', routeUrl('home'));
    renderPage('home');
    if (requestedPage !== 'home') {
      window.history.pushState(routeState(requestedPage, 1), '', routeUrl(requestedPage));
      renderPage(requestedPage);
    }

    window.addEventListener('popstate', event => {
      const statePage = event.state?.[HISTORY_PAGE_KEY];
      const hashPage = window.location.hash.slice(1);
      navigateWithHistory(normalizePage(statePage || hashPage), { fromHistory: true });
    });
  }

  function buildPageBack() {
    const topbar = $('.topbar');
    if (!topbar) return;

    pageBackButton = $('.arena-page-back', topbar);
    if (!pageBackButton) {
      pageBackButton = document.createElement('button');
      pageBackButton.type = 'button';
      pageBackButton.className = 'arena-page-back';
      pageBackButton.setAttribute('aria-label', 'Voltar para a página anterior');
      pageBackButton.innerHTML = '<i aria-hidden="true">&#8592;</i><span>Voltar</span>';
      pageBackButton.addEventListener('click', () => {
        if (historyDepth() > 0) window.history.back();
        else window.navigate('home', { replace: true });
      });
      topbar.prepend(pageBackButton);
    }
    syncPageBack();
  }

  function go(page) {
    if (typeof navigate === 'function') navigate(page);
    else {
      $$('.page').forEach(item => item.classList.toggle('active', item.dataset.page === page));
      window.scrollTo({ top: 0, behavior: 'smooth' });
    }
    closeSheet();
    requestAnimationFrame(syncActive);
  }

  function decorate(button) {
    if (!button) return;
    const meta = META[button.dataset.go];
    if (!meta || button.dataset.arenaDecorated === 'true') return;

    button.dataset.arenaDecorated = 'true';
    button.classList.add('arena-nav-item');
    button.title = `${meta[1]} • ${meta[2]}`;
    button.setAttribute('aria-label', meta[1]);
    button.innerHTML = `<i>${meta[0]}</i><span class="arena-nav-copy"><b>${meta[1]}</b><small>${meta[2]}</small></span>`;
  }

  function buildSidebar() {
    const nav = $('.bottom-nav');
    if (!nav) return;

    nav.classList.add('arena-side-nav');
    nav.setAttribute('aria-label', 'Navegação principal da Arena BDA');

    let brand = $('.arena-side-brand', nav);
    if (!brand) {
      brand = document.createElement('div');
      brand.className = 'arena-side-brand';
      brand.innerHTML = `<span class="arena-side-logo"><img src="${iconUrl()}" alt="Grifo da Arena BDA"></span><span><b>Arena BDA</b><small>Central do Clã</small></span>`;
      nav.prepend(brand);
    }

    let heading = $('.arena-side-heading', nav);
    if (!heading) {
      heading = document.createElement('div');
      heading.className = 'arena-side-heading';
      heading.textContent = 'Navegação';
      brand.after(heading);
    }

    const buttons = new Map($$('.nav-btn[data-go]', nav).map(button => [button.dataset.go, button]));
    ORDER.forEach(page => decorate(buttons.get(page)));

    const orderedButtons = ORDER.map(page => buttons.get(page)).filter(Boolean);
    const currentButtons = $$('.nav-btn[data-go]', nav);
    const isOrdered = orderedButtons.length === currentButtons.length && orderedButtons.every((button, index) => button === currentButtons[index]);
    if (!isOrdered) orderedButtons.forEach(button => nav.append(button));

    orderedButtons.forEach(button => {
      button.classList.toggle('arena-side-secondary', SECONDARY_PAGES.includes(button.dataset.go));
    });

    let more = $('.arena-side-more-toggle', nav);
    if (!more) {
      more = document.createElement('button');
      more.type = 'button';
      more.className = 'nav-btn arena-nav-item arena-side-more-toggle';
      more.innerHTML = '<i>⋯</i><span class="arena-nav-copy"><b>Mais</b><small>5 áreas</small></span>';
      more.addEventListener('click', () => setSidebarMore(!nav.classList.contains('arena-side-more-open')));
    }
    more.setAttribute('aria-label', 'Mostrar mais áreas');
    const firstSecondary = orderedButtons.find(button => button.classList.contains('arena-side-secondary'));
    if (firstSecondary) firstSecondary.before(more);
    const secondaryGroup = orderedButtons.filter(button => button.classList.contains('arena-side-secondary'));
    secondaryGroup.forEach((button, index) => {
      button.id = `arenaSecondaryNavigation${index + 1}`;
    });
    more.setAttribute('aria-controls', secondaryGroup.map(button => button.id).join(' '));

    if (!$('.arena-side-footer', nav)) {
      const footer = document.createElement('div');
      footer.className = 'arena-side-footer';
      footer.innerHTML = '<div><b>Site oficial</b><small>arenabda.com.br</small></div>';
      nav.append(footer);
    }
  }

  function setSidebarMore(open) {
    const nav = $('.arena-side-nav');
    const more = $('.arena-side-more-toggle', nav || document);
    if (!nav || !more) return;
    nav.classList.toggle('arena-side-more-open', open);
    more.setAttribute('aria-expanded', open ? 'true' : 'false');
    more.setAttribute('aria-label', open ? 'Ocultar áreas adicionais' : 'Mostrar mais áreas');
  }

  function mobileItem(page) {
    const [icon, label] = META[page];
    const button = document.createElement('button');
    button.type = 'button';
    button.className = 'arena-mobile-item';
    button.dataset.mobileGo = page;
    button.innerHTML = `<i>${icon}</i><span>${label}</span>`;
    button.setAttribute('aria-label', label);
    button.addEventListener('click', () => go(page));
    return button;
  }

  function buildMobile() {
    if (!mobileNav) {
      mobileNav = document.createElement('nav');
      mobileNav.className = 'arena-mobile-nav';
      mobileNav.setAttribute('aria-label', 'Navegação móvel');
      MOBILE_PRIMARY_PAGES.forEach(page => mobileNav.append(mobileItem(page)));

      const more = document.createElement('button');
      more.type = 'button';
      more.className = 'arena-mobile-item';
      more.dataset.mobileMore = 'true';
      more.innerHTML = '<i>☰</i><span>Mais</span>';
      more.setAttribute('aria-label', 'Abrir mais opções');
      more.addEventListener('click', openSheet);
      mobileNav.append(more);
      document.body.append(mobileNav);
    }

    if (!sheet) {
      sheet = document.createElement('div');
      sheet.className = 'arena-nav-sheet-backdrop';
      sheet.setAttribute('aria-hidden', 'true');
      sheet.innerHTML = `
        <section class="arena-nav-sheet" role="dialog" aria-modal="true" aria-label="Mais opções">
          <header><div><span class="eyebrow">Arena BDA</span><h2>Mais opções</h2></div><button type="button" class="arena-sheet-close" aria-label="Fechar">×</button></header>
          <div class="arena-sheet-grid"></div>
          <footer><img src="${iconUrl()}" alt="Grifo"><span><b>Clã BDA</b><small>Todos os campeonatos em uma só arena.</small></span></footer>
        </section>`;

      MOBILE_SECONDARY_PAGES.forEach(page => {
        const [icon, label, description] = META[page];
        const button = document.createElement('button');
        button.type = 'button';
        button.className = 'arena-sheet-item';
        button.dataset.sheetGo = page;
        button.innerHTML = `<i>${icon}</i><span><b>${label}</b><small>${description}</small></span><em>›</em>`;
        button.addEventListener('click', () => go(page));
        $('.arena-sheet-grid', sheet).append(button);
      });

      $('.arena-sheet-close', sheet).addEventListener('click', closeSheet);
      sheet.addEventListener('click', event => event.target === sheet && closeSheet());
      document.body.append(sheet);
    }
  }

  function openSheet() {
    sheet?.classList.add('show');
    sheet?.setAttribute('aria-hidden', 'false');
    document.body.classList.add('arena-sheet-open');
    $('.arena-sheet-close', sheet)?.focus();
  }

  function closeSheet() {
    sheet?.classList.remove('show');
    sheet?.setAttribute('aria-hidden', 'true');
    document.body.classList.remove('arena-sheet-open');
  }

  function syncActive() {
    const page = currentPage();

    setSidebarMore(SECONDARY_PAGES.includes(page));

    $$('.arena-side-nav .nav-btn[data-go]').forEach(button => {
      const active = button.dataset.go === page;
      button.classList.toggle('active', active);
      active ? button.setAttribute('aria-current', 'page') : button.removeAttribute('aria-current');
    });

    $$('.arena-mobile-item[data-mobile-go]').forEach(button => {
      const active = button.dataset.mobileGo === page;
      button.classList.toggle('active', active);
      active ? button.setAttribute('aria-current', 'page') : button.removeAttribute('aria-current');
    });

    const more = $('.arena-mobile-item[data-mobile-more]');
    if (more) {
      const active = MOBILE_SECONDARY_PAGES.includes(page);
      more.classList.toggle('active', active);
      active ? more.setAttribute('aria-current', 'page') : more.removeAttribute('aria-current');
    }

    const sideMore = $('.arena-side-more-toggle');
    if (sideMore) {
      const active = SECONDARY_PAGES.includes(page);
      sideMore.classList.toggle('active', active);
      active ? sideMore.setAttribute('aria-current', 'page') : sideMore.removeAttribute('aria-current');
    }

    syncPageBack();
  }

  function syncMobileViewport() {
    viewportFrame = 0;
    if (!mobileNav) return;
    const viewport = window.visualViewport;
    const coveredHeight = viewport ? Math.max(0, window.innerHeight - viewport.height - viewport.offsetTop) : 0;
    const keyboardOpen = coveredHeight > 140;
    mobileNav.classList.toggle('keyboard-open', keyboardOpen);
    mobileNav.setAttribute('aria-hidden', keyboardOpen ? 'true' : 'false');
  }

  function scheduleViewportSync() {
    if (viewportFrame) return;
    viewportFrame = requestAnimationFrame(syncMobileViewport);
  }

  function init() {
    document.body.classList.add('arena-navigation-ready');
    installHistoryNavigation();
    buildPageBack();
    buildSidebar();
    buildMobile();
    syncActive();
    syncMobileViewport();
  }

  const style = document.createElement('style');
  style.textContent = `
    .arena-side-brand,.arena-side-heading,.arena-side-footer,.arena-mobile-nav,.arena-nav-sheet-backdrop{display:none}
    .topbar>.top-actions{margin-left:auto}
    .arena-page-back{display:inline-flex;align-items:center;justify-content:center;gap:7px;min-width:82px;height:40px;flex:0 0 auto;padding:0 11px;border:1px solid var(--line);border-radius:6px;color:var(--text);background:transparent;font-size:10px;font-weight:750;line-height:1;white-space:nowrap}
    .arena-page-back[hidden]{display:none!important}
    .arena-page-back:hover{border-color:rgba(242,215,125,.42);background:rgba(255,255,255,.04)}
    .arena-page-back:focus-visible{outline:2px solid var(--gold-soft);outline-offset:2px}
    .arena-page-back i{font:700 18px/1 Arial,sans-serif;font-style:normal}

    @media(min-width:981px){
      html{scroll-padding-top:90px}
      body.arena-navigation-ready{display:block!important;min-height:100vh!important;padding:0 18px 0 252px!important;text-align:left!important}
      body.arena-navigation-ready .app-shell{width:100%!important;max-width:1320px!important;margin:0 auto!important}
      body.arena-navigation-ready .topbar{top:12px;margin:12px 0 0;border:1px solid var(--line);border-radius:19px;background:rgba(5,10,8,.88);box-shadow:0 12px 34px rgba(0,0,0,.28)}
      body.arena-navigation-ready main{padding:20px 4px 36px}
      .bottom-nav.arena-side-nav{position:fixed!important;left:14px!important;top:14px!important;bottom:14px!important;z-index:45!important;width:220px!important;height:auto!important;min-height:0!important;transform:none!important;display:flex!important;flex-direction:column!important;gap:6px!important;padding:13px!important;border:1px solid rgba(242,215,125,.25)!important;border-radius:25px!important;background:radial-gradient(circle at 50% 0,rgba(216,178,72,.19),transparent 27%),linear-gradient(180deg,rgba(12,27,19,.98),rgba(4,10,7,.98))!important;box-shadow:0 24px 60px rgba(0,0,0,.52)!important;backdrop-filter:blur(22px)!important;overflow-x:hidden!important;overflow-y:auto!important;scrollbar-width:thin;scrollbar-color:rgba(242,215,125,.28) transparent}
      .bottom-nav.arena-side-nav::-webkit-scrollbar{width:5px}.bottom-nav.arena-side-nav::-webkit-scrollbar-thumb{border-radius:999px;background:rgba(242,215,125,.28)}
      .arena-side-brand{display:flex;align-items:center;gap:11px;padding:8px 7px 16px;margin-bottom:2px;border-bottom:1px solid var(--line)}
      .arena-side-logo{overflow:hidden;width:48px;height:48px;flex:0 0 auto;border:1px solid rgba(242,215,125,.55);border-radius:15px;background:#020503;box-shadow:0 9px 24px rgba(216,178,72,.17)}
      .arena-side-logo img{display:block;width:100%;height:100%;object-fit:cover}
      .arena-side-brand b,.arena-side-brand small{display:block}.arena-side-brand b{font:800 21px/1 "Barlow Condensed",sans-serif;letter-spacing:.05em;text-transform:uppercase}.arena-side-brand small{margin-top:5px;color:var(--gold-soft);font-size:8px;font-weight:800;letter-spacing:.12em;text-transform:uppercase}
      .arena-side-heading{display:block;padding:8px 10px 3px;color:var(--muted);font-size:7px;font-weight:900;letter-spacing:.17em;text-transform:uppercase}
      .arena-side-nav .nav-btn{position:relative;display:grid!important;grid-template-columns:40px 1fr!important;align-items:center!important;justify-items:stretch!important;gap:10px!important;width:100%!important;min-height:54px!important;padding:7px 9px!important;border:1px solid transparent!important;border-radius:15px!important;color:var(--muted)!important;background:transparent!important;text-align:left!important;transition:transform .16s ease,border-color .16s ease,background .16s ease,color .16s ease!important}
      .arena-side-nav .arena-side-secondary{display:none!important}.arena-side-nav.arena-side-more-open .arena-side-secondary{display:grid!important}
      .arena-side-nav .nav-btn:hover{transform:translateX(3px);color:var(--text)!important;border-color:var(--line)!important;background:rgba(255,255,255,.045)!important}
      .arena-side-nav .nav-btn i{display:grid!important;place-items:center!important;width:40px!important;height:40px!important;border:1px solid var(--line)!important;border-radius:12px!important;background:rgba(255,255,255,.04)!important;font-size:19px!important;line-height:1!important}
      .arena-nav-copy{display:block!important;min-width:0!important}.arena-nav-copy b,.arena-nav-copy small{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.arena-nav-copy b{font-size:10px}.arena-nav-copy small{margin-top:4px;color:var(--muted);font-size:7px;font-weight:500}
      .arena-side-nav .nav-btn.active{color:#171107!important;border-color:var(--gold)!important;background:linear-gradient(135deg,var(--gold-soft),var(--gold))!important;box-shadow:0 10px 24px rgba(216,178,72,.18)!important}.arena-side-nav .nav-btn.active i{border-color:rgba(23,17,7,.14)!important;background:rgba(255,255,255,.28)!important}.arena-side-nav .nav-btn.active .arena-nav-copy small{color:rgba(23,17,7,.68)!important}.arena-side-nav .nav-btn.active:after{content:"";position:absolute;right:8px;width:5px;height:5px;border-radius:50%;background:#171107;box-shadow:0 0 0 4px rgba(23,17,7,.11)}
      .arena-side-footer{display:flex;align-items:center;gap:9px;margin-top:auto;padding:11px 9px;border:1px solid var(--line);border-radius:14px;background:rgba(255,255,255,.035)}.arena-side-footer>span{color:var(--green);font-size:10px;text-shadow:0 0 12px var(--green)}.arena-side-footer b,.arena-side-footer small{display:block}.arena-side-footer b{font-size:8px;text-transform:uppercase}.arena-side-footer small{margin-top:3px;color:var(--muted);font-size:7px}
    }

    @media(min-width:981px) and (max-height:740px){
      .bottom-nav.arena-side-nav{gap:3px!important;padding-block:9px!important}.arena-side-brand{padding-bottom:10px}.arena-side-logo{width:42px;height:42px}.arena-side-nav .nav-btn{min-height:47px!important;padding-block:4px!important}.arena-side-nav .nav-btn i{width:35px!important;height:35px!important;font-size:17px!important}.arena-nav-copy small{display:none}.arena-side-footer{padding-block:8px}
    }

    @media(max-width:980px){
      body.arena-navigation-ready{padding-bottom:calc(74px + env(safe-area-inset-bottom))!important}
      .bottom-nav.arena-side-nav{display:none!important}
      .arena-page-back{width:40px;min-width:40px;height:40px;padding:0;border-radius:6px}
      .arena-page-back span{position:absolute;width:1px;height:1px;overflow:hidden;clip-path:inset(50%);white-space:nowrap}
      .arena-mobile-nav{position:fixed;left:8px;right:8px;bottom:max(6px,env(safe-area-inset-bottom));z-index:48;display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:3px;height:62px;padding:5px;border:1px solid rgba(242,215,125,.20);border-radius:19px;background:linear-gradient(180deg,rgba(15,31,22,.97),rgba(5,12,8,.985));box-shadow:0 12px 34px rgba(0,0,0,.48);backdrop-filter:blur(14px);transition:opacity .16s ease,transform .16s ease,visibility .16s}
      .arena-mobile-nav.keyboard-open{opacity:0;visibility:hidden;pointer-events:none;transform:translateY(calc(100% + 18px))}
      .arena-mobile-item{display:grid;place-items:center;align-content:center;gap:3px;min-width:0;min-height:50px;padding:3px 2px;border:0;border-radius:13px;color:var(--muted);background:transparent;font-size:7px;font-weight:800;line-height:1}.arena-mobile-item i{display:grid;place-items:center;width:28px;height:26px;border-radius:9px;font-style:normal;font-size:16px;line-height:1}.arena-mobile-item span{overflow:hidden;width:100%;text-overflow:ellipsis;white-space:nowrap}.arena-mobile-item.active{color:#171107;background:linear-gradient(135deg,var(--gold-soft),var(--gold));box-shadow:0 6px 14px rgba(216,178,72,.15)}.arena-mobile-item.active i{background:rgba(255,255,255,.22)}
      .arena-nav-sheet-backdrop{position:fixed;inset:0;z-index:70;display:grid;align-items:end;padding:12px 12px max(12px,env(safe-area-inset-bottom));background:rgba(0,0,0,.68);opacity:0;visibility:hidden;backdrop-filter:blur(7px);transition:.2s}.arena-nav-sheet-backdrop.show{opacity:1;visibility:visible}.arena-nav-sheet{overflow-y:auto;width:min(100%,520px);max-height:calc(100dvh - 24px - env(safe-area-inset-bottom));margin:0 auto;padding:18px;border:1px solid rgba(242,215,125,.28);border-radius:24px;background:radial-gradient(circle at 90% 0,rgba(216,178,72,.18),transparent 31%),linear-gradient(155deg,#132a1d,#07100c);box-shadow:0 24px 70px rgba(0,0,0,.65);transform:translateY(18px);transition:.2s}.arena-nav-sheet-backdrop.show .arena-nav-sheet{transform:none}
      .arena-nav-sheet header{display:flex;align-items:center;justify-content:space-between;gap:12px;padding-bottom:13px;border-bottom:1px solid var(--line)}.arena-nav-sheet h2{margin:3px 0 0;font-size:27px;text-transform:uppercase}.arena-sheet-close{width:42px;height:42px;border:1px solid var(--line);border-radius:13px;color:var(--text);background:rgba(255,255,255,.05);font-size:24px}.arena-sheet-grid{display:grid;gap:8px;margin:13px 0}
      .arena-sheet-item{display:grid;grid-template-columns:44px 1fr auto;align-items:center;gap:10px;width:100%;min-height:62px;padding:8px 11px;border:1px solid var(--line);border-radius:16px;color:var(--text);background:rgba(255,255,255,.035);text-align:left}.arena-sheet-item i{display:grid;place-items:center;width:43px;height:43px;border:1px solid rgba(242,215,125,.25);border-radius:13px;background:rgba(216,178,72,.08);font-style:normal;font-size:21px}.arena-sheet-item b,.arena-sheet-item small{display:block}.arena-sheet-item b{font-size:11px}.arena-sheet-item small{margin-top:4px;color:var(--muted);font-size:8px}.arena-sheet-item em{color:var(--gold-soft);font-size:26px;font-style:normal}
      .arena-nav-sheet footer{display:flex;align-items:center;gap:10px;padding-top:12px;border-top:1px solid var(--line)}.arena-nav-sheet footer img{width:40px;height:40px;border:1px solid rgba(242,215,125,.4);border-radius:12px;object-fit:cover}.arena-nav-sheet footer b,.arena-nav-sheet footer small{display:block}.arena-nav-sheet footer b{font-size:9px;text-transform:uppercase}.arena-nav-sheet footer small{margin-top:3px;color:var(--muted);font-size:8px}body.arena-sheet-open{overflow:hidden}
    }

    @media(max-width:390px){.arena-page-back{width:38px;min-width:38px;height:38px}.arena-mobile-nav{left:5px;right:5px;gap:2px;padding-inline:4px}.arena-mobile-item{font-size:6.5px}.arena-mobile-item i{width:27px;height:25px;font-size:15px}}
    @media(prefers-reduced-transparency:reduce){.arena-mobile-nav,.arena-nav-sheet-backdrop{backdrop-filter:none}}
  `;
  document.head.append(style);

  document.addEventListener('click', event => {
    if (event.target.closest('[data-go], [data-mobile-go], [data-sheet-go]')) setTimeout(syncActive, 20);
  });
  document.addEventListener('keydown', event => {
    if (event.key !== 'Escape') return;
    closeSheet();
    setSidebarMore(false);
  });
  window.visualViewport?.addEventListener('resize', scheduleViewportSync, { passive: true });
  window.visualViewport?.addEventListener('scroll', scheduleViewportSync, { passive: true });
  window.addEventListener('resize', scheduleViewportSync, { passive: true });

  const pageObserver = new MutationObserver(syncActive);
  $$('.page').forEach(page => pageObserver.observe(page, { attributes: true, attributeFilter: ['class'] }));

  init();
})();

;

/* ===== Source: arena-design-system.js ===== */
/* Arena BDA Design System — camada visual consolidada. */
/* ===== design-arena-pro.js ===== */
(() => {
  'use strict';

  const $ = (selector, root = document) => root.querySelector(selector);
  const $$ = (selector, root = document) => [...root.querySelectorAll(selector)];

  document.documentElement.dataset.arenaDesign = 'pro-2026';
  document.body.classList.add('arena-design-pro');

  function currentPage() {
    return $('.page.active')?.dataset.page || 'home';
  }

  function syncPageTheme() {
    document.body.dataset.currentPage = currentPage();
  }

  function installTopStatus() {
    const topbar = $('.topbar');
    const actions = $('.top-actions', topbar);
    if (!topbar || !actions || $('.arena-top-status', topbar)) return;

    const status = document.createElement('div');
    status.className = 'arena-top-status';
    status.innerHTML = '<i></i><span>Clã BDA online</span>';
    actions.prepend(status);
  }

  function installProgress() {
    if ($('#arenaScrollProgress')) return;
    const progress = document.createElement('div');
    progress.id = 'arenaScrollProgress';
    progress.setAttribute('aria-hidden', 'true');
    progress.innerHTML = '<i></i>';
    document.body.append(progress);

    let frame = 0;
    const update = () => {
      frame = 0;
      const total = Math.max(1, document.documentElement.scrollHeight - innerHeight);
      const percent = Math.min(100, Math.max(0, scrollY / total * 100));
      progress.style.setProperty('--arena-progress', `${percent}%`);
    };
    const schedule = () => {
      if (!frame) frame = requestAnimationFrame(update);
    };
    addEventListener('scroll', schedule, { passive: true });
    addEventListener('resize', schedule, { passive: true });
    update();
  }

  function installBackToTop() {
    if ($('#arenaBackToTop')) return;
    const button = document.createElement('button');
    button.id = 'arenaBackToTop';
    button.type = 'button';
    button.setAttribute('aria-label', 'Voltar ao topo');
    button.innerHTML = '<span>↑</span>';
    button.addEventListener('click', () => scrollTo({ top: 0, behavior: 'smooth' }));
    document.body.append(button);

    let frame = 0;
    const update = () => {
      frame = 0;
      button.classList.toggle('show', scrollY > 520);
    };
    const schedule = () => {
      if (!frame) frame = requestAnimationFrame(update);
    };
    addEventListener('scroll', schedule, { passive: true });
    update();
  }

  function installRipple() {
    document.addEventListener('pointerdown', event => {
      const button = event.target.closest('button');
      if (
        !button || button.disabled || button.closest('.history-gallery-media')
        || document.documentElement.classList.contains('arena-performance-lite')
        || matchMedia('(pointer:coarse)').matches
      ) return;
      const rect = button.getBoundingClientRect();
      const size = Math.max(rect.width, rect.height) * 1.45;
      const ripple = document.createElement('span');
      ripple.className = 'arena-button-ripple';
      ripple.style.width = ripple.style.height = `${size}px`;
      ripple.style.left = `${event.clientX - rect.left - size / 2}px`;
      ripple.style.top = `${event.clientY - rect.top - size / 2}px`;
      button.append(ripple);
      ripple.addEventListener('animationend', () => ripple.remove(), { once: true });
    });
  }

  function enhanceImages() {
    $$('img').forEach(image => {
      if (!image.hasAttribute('decoding')) image.decoding = 'async';
      if (!image.closest('.hero,.topbar,.arena-side-brand,.history-hero,.rank-hero') && !image.hasAttribute('loading')) {
        image.loading = 'lazy';
      }
    });
  }

  function enhanceStructure() {
    installTopStatus();
    enhanceImages();
    syncPageTheme();
  }

  const style = document.createElement('style');
  style.textContent = `
    :root{
      --bg:#030805;
      --bg-soft:#07110b;
      --surface:rgba(11,25,17,.92);
      --surface-2:rgba(17,38,25,.90);
      --surface-3:rgba(25,51,35,.74);
      --line:rgba(219,241,226,.115);
      --line-strong:rgba(242,215,125,.38);
      --text:#f7faf6;
      --muted:#a6b8ad;
      --gold:#d3a93a;
      --gold-soft:#f5dc86;
      --green:#58e59a;
      --red:#ff7280;
      --blue:#77bdff;
      --shadow:0 24px 70px rgba(0,0,0,.40);
      --shadow-soft:0 14px 38px rgba(0,0,0,.24);
      --radius:22px;
      --radius-small:15px;
    }

    *{scrollbar-width:thin;scrollbar-color:rgba(242,215,125,.30) rgba(255,255,255,.02)}
    *::-webkit-scrollbar{width:8px;height:8px}
    *::-webkit-scrollbar-track{background:rgba(255,255,255,.02)}
    *::-webkit-scrollbar-thumb{border:2px solid transparent;border-radius:999px;background:rgba(242,215,125,.28);background-clip:padding-box}
    *::-webkit-scrollbar-thumb:hover{background:rgba(242,215,125,.46);background-clip:padding-box}

    body.arena-design-pro{
      position:relative;
      isolation:isolate;
      color:var(--text);
      background:
        radial-gradient(circle at 78% -8%,rgba(242,215,125,.18),transparent 29%),
        radial-gradient(circle at 8% 34%,rgba(88,229,154,.08),transparent 27%),
        radial-gradient(circle at 92% 78%,rgba(109,182,255,.05),transparent 23%),
        linear-gradient(180deg,#08130d 0,#030805 47%,#020503 100%)!important;
    }
    body.arena-design-pro:before{
      content:"";position:fixed;inset:0;z-index:-2;pointer-events:none;opacity:.34;
      background-image:linear-gradient(rgba(255,255,255,.018) 1px,transparent 1px),linear-gradient(90deg,rgba(255,255,255,.018) 1px,transparent 1px);
      background-size:42px 42px;
      mask-image:linear-gradient(to bottom,black,transparent 78%);
    }
    body.arena-design-pro:after{
      content:"";position:fixed;inset:0;z-index:-1;pointer-events:none;
      background:linear-gradient(115deg,transparent 0 48%,rgba(242,215,125,.025) 49%,transparent 50% 100%);
      background-size:260px 260px;
    }

    ::selection{color:#171107;background:var(--gold-soft)}
    a{color:var(--gold-soft)}
    button{position:relative;overflow:hidden;-webkit-tap-highlight-color:transparent}
    button:disabled{cursor:not-allowed;opacity:.48;filter:saturate(.5)}
    .arena-button-ripple{position:absolute;z-index:0;border-radius:50%;pointer-events:none;background:rgba(255,255,255,.23);transform:scale(0);animation:arenaRipple .55s ease-out}
    button>*{position:relative;z-index:1}
    @keyframes arenaRipple{to{transform:scale(1);opacity:0}}

    #arenaScrollProgress{position:fixed;left:0;right:0;top:0;z-index:140;height:3px;pointer-events:none;background:rgba(255,255,255,.025)}
    #arenaScrollProgress i{display:block;width:var(--arena-progress,0%);height:100%;background:linear-gradient(90deg,var(--green),var(--gold-soft),var(--gold));box-shadow:0 0 16px rgba(242,215,125,.58);transition:width .08s linear}
    #arenaBackToTop{position:fixed;right:18px;bottom:22px;z-index:47;display:grid;place-items:center;width:44px;height:44px;border:1px solid rgba(242,215,125,.42);border-radius:15px;color:#171107;background:linear-gradient(145deg,var(--gold-soft),var(--gold));box-shadow:0 12px 28px rgba(0,0,0,.36);opacity:0;visibility:hidden;transform:translateY(12px) scale(.92);transition:.2s}
    #arenaBackToTop.show{opacity:1;visibility:visible;transform:none}
    #arenaBackToTop:hover{transform:translateY(-3px)}
    #arenaBackToTop span{font-size:21px;font-weight:900}

    .app-shell{position:relative}
    .topbar{
      min-height:74px!important;padding:11px 14px!important;border:1px solid rgba(219,241,226,.105)!important;
      background:linear-gradient(180deg,rgba(9,22,14,.92),rgba(4,10,7,.84))!important;
      box-shadow:0 18px 46px rgba(0,0,0,.24)!important;backdrop-filter:blur(22px) saturate(1.25)!important;
    }
    .topbar:after{content:"";position:absolute;left:18px;right:18px;bottom:-1px;height:1px;background:linear-gradient(90deg,transparent,var(--gold-soft),transparent);opacity:.24}
    .brand-mark{overflow:hidden;border:1px solid rgba(242,215,125,.48)!important;background:linear-gradient(145deg,#152b1d,#030806)!important;box-shadow:0 10px 27px rgba(0,0,0,.34),inset 0 0 18px rgba(242,215,125,.10)!important;transform:none!important}
    .brand-mark img{width:100%;height:100%;object-fit:cover}
    .brand-copy strong{font-size:23px!important;letter-spacing:.075em!important}
    .brand-copy span{color:#b8c6bd!important;font-size:8px!important;font-weight:700!important}
    .top-actions{gap:7px!important}
    .arena-top-status{display:flex;align-items:center;gap:7px;min-height:38px;padding:0 11px;border:1px solid var(--line);border-radius:999px;color:var(--muted);background:rgba(255,255,255,.035);font-size:8px;font-weight:800;text-transform:uppercase;letter-spacing:.08em}
    .arena-top-status i{width:7px;height:7px;border-radius:50%;background:var(--green);box-shadow:0 0 0 5px rgba(88,229,154,.10),0 0 13px rgba(88,229,154,.55)}
    .icon-btn,.admin-btn{border-color:rgba(219,241,226,.13)!important;background:linear-gradient(145deg,rgba(24,48,33,.84),rgba(7,16,11,.90))!important;box-shadow:inset 0 1px 0 rgba(255,255,255,.035),0 8px 22px rgba(0,0,0,.16)}
    .icon-btn:hover,.admin-btn:hover{border-color:rgba(242,215,125,.42)!important;transform:translateY(-1px)}
    .admin-btn.active{color:#171107!important;background:linear-gradient(135deg,var(--gold-soft),var(--gold))!important}

    main{position:relative;padding-top:20px!important}
    .page.active{animation:arenaPageIn .34s cubic-bezier(.2,.8,.2,1)}
    @keyframes arenaPageIn{from{opacity:0;transform:translateY(12px) scale(.995)}to{opacity:1;transform:none}}

    .hero,.arena-hero,.rank-hero,.history-hero,.league-generator-head{
      border-color:rgba(242,215,125,.34)!important;
      box-shadow:0 28px 80px rgba(0,0,0,.42),inset 0 1px 0 rgba(255,255,255,.05)!important;
    }
    .hero{min-height:330px!important;padding:clamp(22px,4vw,34px)!important;border-radius:31px!important;background:linear-gradient(180deg,transparent 0 22%,rgba(3,8,5,.92) 84%),radial-gradient(circle at 78% 19%,rgba(245,220,134,.42),transparent 20%),linear-gradient(135deg,#1c4b30,#08140d 66%)!important}
    .hero:before{opacity:.18!important;filter:grayscale(.2) contrast(1.2) drop-shadow(0 20px 28px rgba(0,0,0,.4))!important}
    .hero:after{background:linear-gradient(110deg,transparent 0 55%,rgba(255,255,255,.028) 56%,transparent 57%),repeating-linear-gradient(115deg,transparent 0 46px,rgba(255,255,255,.014) 46px 48px)!important}
    .hero h1{max-width:800px;margin-top:10px!important;font-size:clamp(45px,8vw,76px)!important;letter-spacing:-.025em!important;text-wrap:balance}
    .hero p{font-size:12px!important;line-height:1.68!important}
    .eyebrow{display:inline-flex;align-items:center;gap:7px;color:var(--gold-soft)!important;font-size:9px!important;font-weight:900!important;letter-spacing:.18em!important}
    .eyebrow:before{content:"";width:18px;height:2px;border-radius:999px;background:linear-gradient(90deg,var(--gold-soft),transparent)}

    .primary,.secondary,.ghost,.danger{
      min-height:43px!important;border-radius:13px!important;padding-inline:15px!important;font-size:10px!important;letter-spacing:.025em!important;
      transition:transform .16s ease,border-color .16s ease,box-shadow .16s ease,filter .16s ease!important;
    }
    .primary{background:linear-gradient(135deg,#f9e8a7 0,var(--gold-soft) 33%,var(--gold) 100%)!important;box-shadow:0 12px 27px rgba(211,169,58,.20),inset 0 1px 0 rgba(255,255,255,.48)!important}
    .secondary{border-color:rgba(219,241,226,.14)!important;background:linear-gradient(145deg,rgba(255,255,255,.07),rgba(255,255,255,.035))!important}
    .ghost{border-color:rgba(242,215,125,.32)!important;background:rgba(242,215,125,.025)!important}
    .danger{background:linear-gradient(145deg,rgba(255,114,128,.15),rgba(255,114,128,.07))!important}
    .primary:hover,.secondary:hover,.ghost:hover,.danger:hover{transform:translateY(-2px);filter:brightness(1.06)}
    .primary:active,.secondary:active,.ghost:active,.danger:active{transform:translateY(0) scale(.985)}

    .section-head{position:relative;margin-top:30px!important;margin-bottom:13px!important;padding-left:13px}
    .section-head:before{content:"";position:absolute;left:0;top:1px;bottom:1px;width:3px;border-radius:999px;background:linear-gradient(var(--gold-soft),var(--gold));box-shadow:0 0 13px rgba(242,215,125,.28)}
    .section-head h2{font-size:clamp(24px,4vw,31px)!important;line-height:.95!important;letter-spacing:.025em!important}
    .section-head p{margin-top:6px!important;font-size:9px!important;line-height:1.45!important}
    .section-head>button{min-height:35px;padding:0 10px;border:1px solid var(--line)!important;border-radius:11px!important;background:rgba(255,255,255,.035)!important;font-size:9px!important}

    :where(.card,.arena-card,.champion-card,.team-card,.stat,.arena-stat,.gi-game,.rank-podium-card,.rank-hall article,.rank-rules article,.league-groups-preview article,.history-values article,.history-timeline article,.history-gallery-card,.registration-card,.registration-admin-card,.auto-standing-card){
      border-color:rgba(219,241,226,.105)!important;
      background:linear-gradient(152deg,rgba(24,49,33,.82),rgba(8,19,13,.94))!important;
      box-shadow:0 14px 38px rgba(0,0,0,.22),inset 0 1px 0 rgba(255,255,255,.025)!important;
      transition:transform .2s ease,border-color .2s ease,box-shadow .2s ease!important;
    }
    :where(.card,.arena-card,.champion-card,.team-card,.stat,.arena-stat,.gi-game,.rank-podium-card,.league-groups-preview article,.history-values article,.history-gallery-card):hover{
      border-color:rgba(242,215,125,.26)!important;box-shadow:0 20px 52px rgba(0,0,0,.29),inset 0 1px 0 rgba(255,255,255,.04)!important;
    }
    @media(hover:hover) and (pointer:fine){
      :where(.arena-card,.champion-card,.team-card,.stat,.arena-stat,.gi-game,.rank-podium-card,.league-groups-preview article,.history-values article,.history-gallery-card):hover{transform:translateY(-4px)}
    }

    .quick-stats{gap:9px!important;margin:16px 0!important}
    .stat{position:relative;overflow:hidden;min-height:91px;padding:16px!important;border-radius:18px!important}
    .stat:after{content:"";position:absolute;right:-18px;bottom:-27px;width:65px;height:65px;border:1px solid rgba(242,215,125,.09);border-radius:50%}
    .stat b{font-size:31px!important;text-shadow:0 6px 20px rgba(242,215,125,.13)}
    .stat span{font-size:8px!important}

    .live-card,.match-list,.rank-table-wrap{overflow:hidden;border-radius:21px!important}
    .live-top{background:linear-gradient(90deg,rgba(255,114,128,.06),transparent)!important}
    .versus{min-height:155px!important;padding:23px 16px!important}
    .club-badge,.team-mini-badge,.rank-badge,.gi-badge{box-shadow:0 10px 24px rgba(0,0,0,.31),inset 0 1px 0 rgba(255,255,255,.34)!important}
    .match-row{min-height:66px!important;padding:13px 14px!important;transition:background .16s ease}
    .match-row:hover{background:rgba(255,255,255,.025)}

    .arena-cover,.champion-banner-frame,.history-gallery-media{background:#020503!important}
    .arena-cover img,.champion-banner-image,.history-gallery-media img{filter:saturate(1.04) contrast(1.02)}
    .arena-body h3,.champion-card h3,.team-card h3,.history-gallery-copy h3{text-wrap:balance}
    .arena-meta span,.arena-club{border-color:rgba(219,241,226,.10)!important;background:rgba(255,255,255,.035)!important}

    input,select,textarea{
      min-height:45px;border-color:rgba(219,241,226,.13)!important;border-radius:13px!important;
      color:var(--text)!important;background:linear-gradient(145deg,rgba(2,7,4,.72),rgba(9,21,14,.78))!important;
      box-shadow:inset 0 1px 0 rgba(255,255,255,.02)!important;transition:border-color .16s ease,box-shadow .16s ease,background .16s ease!important;
    }
    textarea{line-height:1.55!important}
    input:hover,select:hover,textarea:hover{border-color:rgba(242,215,125,.26)!important}
    input:focus,select:focus,textarea:focus{border-color:var(--gold-soft)!important;outline:none!important;box-shadow:0 0 0 3px rgba(242,215,125,.10),inset 0 1px 0 rgba(255,255,255,.025)!important}
    label{color:#aec0b5!important;font-size:8px!important;font-weight:800!important;letter-spacing:.095em!important}
    option{color:var(--text);background:#0b1a11}

    .modal-backdrop{background:rgba(0,0,0,.76)!important;backdrop-filter:blur(11px)!important}
    .modal{border-color:rgba(242,215,125,.34)!important;background:radial-gradient(circle at 100% 0,rgba(242,215,125,.12),transparent 30%),linear-gradient(150deg,#142d1e,#06100a)!important;box-shadow:0 34px 100px rgba(0,0,0,.66)!important}
    .modal h2{font-size:31px!important;text-transform:uppercase}
    .toast{border:1px solid rgba(242,215,125,.32)!important;border-radius:14px!important;background:linear-gradient(145deg,#173c27,#07100c)!important;box-shadow:0 18px 45px rgba(0,0,0,.48)!important}
    .empty,.rank-empty,.gi-empty,.history-empty,.history-gallery-empty,.league-empty{border-color:rgba(242,215,125,.22)!important;background:linear-gradient(145deg,rgba(242,215,125,.035),rgba(255,255,255,.018))!important}

    .admin-panel{border-color:rgba(242,215,125,.35)!important;border-radius:20px!important;background:radial-gradient(circle at 90% 0,rgba(242,215,125,.12),transparent 32%),linear-gradient(145deg,rgba(33,66,45,.78),rgba(8,20,13,.88))!important;box-shadow:var(--shadow-soft)!important}
    .admin-tools{gap:7px!important}

    table{font-variant-numeric:tabular-nums}
    .rank-table-wrap{box-shadow:var(--shadow-soft)!important}
    .rank-table thead th{background:rgba(4,11,7,.96)!important;backdrop-filter:blur(12px)}
    .rank-table tbody tr{transition:background .14s ease}
    .rank-table tbody tr:nth-child(even){background:rgba(255,255,255,.012)}
    .rank-table tbody tr:hover{background:rgba(242,215,125,.045)!important}

    #giManager>nav,.rank-toolbar,.arena-toolbar,.history-gallery-toolbar{
      border-color:rgba(219,241,226,.105)!important;background:rgba(4,11,7,.78)!important;box-shadow:0 12px 30px rgba(0,0,0,.20)!important;backdrop-filter:blur(17px)!important;
    }
    #giManager>nav button,.rank-toolbar nav button,.arena-filter,.history-gallery-filters button{transition:background .16s ease,color .16s ease,transform .16s ease!important}
    #giManager>nav button:hover,.rank-toolbar nav button:hover,.arena-filter:hover,.history-gallery-filters button:hover{transform:translateY(-1px);color:var(--text)!important}

    .history-story p,.history-timeline-copy p,.history-gallery-copy p,.arena-hero-copy p,.rank-hero p{color:#c8d5cd!important}
    .history-quote{box-shadow:0 20px 54px rgba(0,0,0,.30)!important}
    .history-gallery-lightbox{background:rgba(0,0,0,.89)!important;backdrop-filter:blur(15px)!important}

    .bottom-nav.arena-side-nav{border-color:rgba(242,215,125,.28)!important;background:radial-gradient(circle at 50% 0,rgba(242,215,125,.15),transparent 25%),linear-gradient(180deg,rgba(12,29,19,.98),rgba(3,9,6,.985))!important;box-shadow:0 30px 80px rgba(0,0,0,.53)!important}
    .arena-side-nav .nav-btn{transition:transform .16s ease,border-color .16s ease,background .16s ease!important}
    .arena-side-nav .nav-btn:not(.active):hover{background:rgba(255,255,255,.045)!important;border-color:rgba(242,215,125,.18)!important}
    .arena-mobile-nav{border-color:rgba(242,215,125,.29)!important;background:linear-gradient(180deg,rgba(15,35,23,.97),rgba(4,11,7,.985))!important;box-shadow:0 22px 58px rgba(0,0,0,.60)!important}

    @media(min-width:981px){
      body.arena-design-pro .topbar{margin-top:14px!important;border-radius:21px!important}
      body.arena-design-pro main{padding-inline:8px!important;padding-bottom:48px!important}
      #arenaBackToTop{right:24px;bottom:24px}
    }

    @media(max-width:980px){
      #arenaBackToTop{right:14px;bottom:94px;width:40px;height:40px;border-radius:13px}
      .topbar{margin:8px 8px 0;border-radius:18px!important;min-height:67px!important;padding:9px 10px!important}
      main{padding:14px 12px 22px!important}
      .hero{min-height:350px!important;border-radius:25px!important}
      .arena-top-status{display:none}
      .brand-copy strong{font-size:20px!important}
      .brand-copy span{max-width:170px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
      .quick-stats{grid-template-columns:repeat(3,1fr)!important}
      .stat{min-height:79px;padding:13px 10px!important}
      .stat b{font-size:26px!important}
      .section-head{align-items:start!important}
    }

    @media(max-width:620px){
      .hero{min-height:390px!important;padding:21px!important}
      .hero h1{font-size:clamp(40px,15vw,62px)!important}
      .hero-actions{display:grid!important;grid-template-columns:1fr 1fr!important}
      .hero-actions button{width:100%}
      .quick-stats{gap:7px!important}
      .stat{border-radius:15px!important}
      .section-head{display:grid!important}
      .section-head>button{justify-self:start}
      .versus{gap:7px!important;padding-inline:8px!important}
      .club-badge{width:50px!important;height:50px!important}
      .scorebox b{font-size:21px!important}
      .form-actions{display:grid!important;grid-template-columns:1fr 1fr!important}
      .modal{padding:17px!important;border-radius:22px!important}
    }

    @media(max-width:390px){
      .hero-actions{grid-template-columns:1fr!important}
      .quick-stats{grid-template-columns:repeat(2,1fr)!important}
      .top-actions{gap:5px!important}.icon-btn{width:38px!important}.admin-btn{padding-inline:9px!important;font-size:9px!important}
      .brand-mark{width:42px!important;height:42px!important}
    }

    @media(prefers-reduced-motion:reduce){
      *,*:before,*:after{scroll-behavior:auto!important;animation-duration:.01ms!important;animation-iteration-count:1!important;transition-duration:.01ms!important}
    }
  `;
  document.head.append(style);

  installProgress();
  installBackToTop();
  installRipple();
  enhanceStructure();

  window.ArenaDOMEvents.subscribe(() => requestAnimationFrame(enhanceStructure), { selector: 'body,.app-view,.bottom-nav,.arena-card' });
})();

/* ===== Arena Experience V5: Home e Campeonatos ===== */
(() => {
  'use strict';
  document.documentElement.dataset.arenaExperience = 'v5';
  const style = document.createElement('style');
  style.id = 'arenaExperienceV5Styles';
  style.textContent = `
    :root{--v5-panel:rgba(11,27,18,.92);--v5-panel-deep:rgba(4,12,8,.96);--v5-stroke:rgba(242,215,125,.20);--v5-soft:rgba(255,255,255,.055)}
    [data-page="home"]{display:none;gap:18px}[data-page="home"].active{display:grid}
    [data-page="home"] .hero{position:relative;isolation:isolate;overflow:hidden;min-height:clamp(390px,58vh,610px);align-content:end;padding:clamp(24px,5vw,58px);border:1px solid var(--v5-stroke);border-radius:32px;background:radial-gradient(circle at 75% 22%,rgba(242,215,125,.25),transparent 25%),radial-gradient(circle at 8% 90%,rgba(52,205,123,.14),transparent 30%),linear-gradient(135deg,#173f29 0%,#091b12 48%,#030806 100%);box-shadow:0 30px 80px rgba(0,0,0,.46)}
    [data-page="home"] .hero:before{content:"BDA";position:absolute;right:-2vw;bottom:-9vw;z-index:-1;color:rgba(255,255,255,.025);font:900 clamp(190px,35vw,480px)/.8 "Barlow Condensed",sans-serif;letter-spacing:-.06em}
    [data-page="home"] .hero:after{content:"";position:absolute;right:clamp(24px,7vw,86px);top:clamp(28px,7vw,72px);z-index:-1;width:clamp(110px,18vw,210px);aspect-ratio:1;border:1px solid rgba(242,215,125,.20);border-radius:42px;background:linear-gradient(145deg,rgba(242,215,125,.12),rgba(255,255,255,.02));box-shadow:0 20px 70px rgba(0,0,0,.35);transform:rotate(8deg)}
    [data-page="home"] .hero-content{max-width:780px}
    [data-page="home"] .hero .eyebrow{display:inline-flex;align-items:center;gap:7px;padding:7px 10px;border:1px solid rgba(242,215,125,.25);border-radius:999px;background:rgba(3,8,6,.35);backdrop-filter:blur(10px)}
    [data-page="home"] .hero h1{max-width:760px;margin:13px 0 12px;font-size:clamp(52px,10vw,112px);line-height:.78;letter-spacing:-.035em;text-wrap:balance}
    [data-page="home"] .hero p{max-width:660px;color:#d6e2da;font-size:clamp(12px,1.5vw,16px);line-height:1.65}
    [data-page="home"] .hero-actions{display:flex;flex-wrap:wrap;gap:9px;margin-top:22px}
    [data-page="home"] .hero-actions button{min-height:50px;padding:0 18px;border-radius:14px;font-size:10px}
    #arenaStats{grid-template-columns:repeat(6,minmax(0,1fr));gap:8px;margin:0}
    #arenaStats .stat{min-height:91px;display:grid;align-content:center;padding:14px;border:1px solid var(--line);border-radius:18px;background:linear-gradient(150deg,var(--v5-panel),var(--v5-panel-deep));box-shadow:none}
    #arenaStats .stat b{font-size:26px}
    .home-command{display:grid;gap:11px;padding:18px;border:1px solid var(--v5-stroke);border-radius:25px;background:linear-gradient(145deg,var(--v5-panel),var(--v5-panel-deep));box-shadow:0 18px 50px rgba(0,0,0,.24)}
    .home-command>header{display:flex;align-items:end;justify-content:space-between;gap:12px}.home-command h2{margin:4px 0 0;font-size:clamp(25px,4vw,38px);text-transform:uppercase}
    .home-online{display:flex;align-items:center;gap:7px;padding:7px 9px;border:1px solid rgba(79,223,143,.20);border-radius:999px;color:var(--green);background:rgba(79,223,143,.06);font-size:8px;font-weight:900;text-transform:uppercase}.home-online i{width:7px;height:7px;border-radius:50%;background:currentColor;box-shadow:0 0 12px currentColor}
    .home-command nav{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:8px}.home-command nav button{display:grid;grid-template-columns:43px minmax(0,1fr) auto;align-items:center;gap:10px;min-height:78px;padding:11px;border:1px solid var(--line);border-radius:17px;color:var(--text);background:rgba(255,255,255,.035);text-align:left;transition:.18s}.home-command nav button:hover{transform:translateY(-2px);border-color:rgba(242,215,125,.38);background:rgba(242,215,125,.07)}.home-command nav>button>i{display:grid;place-items:center;width:43px;height:43px;border:1px solid var(--line);border-radius:13px;background:rgba(255,255,255,.05);font-style:normal;font-size:21px}.home-command nav span,.home-command nav b,.home-command nav small{display:block;min-width:0}.home-command nav b{font-size:10px}.home-command nav small{overflow:hidden;margin-top:3px;color:var(--muted);font-size:7px;text-overflow:ellipsis;white-space:nowrap}.home-command nav em{color:var(--gold-soft);font-size:16px;font-style:normal}
    .home-tournaments{display:grid;gap:9px}.home-tournaments>.section-head{margin:0}
    .arena-home-scroll{grid-auto-columns:minmax(245px,31%)!important;gap:11px!important;scroll-snap-type:x proximity}.arena-home-card{min-height:185px!important;scroll-snap-align:start;padding:17px!important;border-radius:21px!important;background:radial-gradient(circle at 100% 0,rgba(242,215,125,.16),transparent 34%),linear-gradient(150deg,var(--v5-panel),var(--v5-panel-deep))!important}.arena-home-card h3{font-size:28px!important}.arena-home-card button{min-height:40px!important}

    .arena-page-hero{position:relative;overflow:hidden;display:grid;grid-template-columns:minmax(0,1.4fr) minmax(220px,.6fr);align-items:end;gap:22px;min-height:285px;padding:clamp(24px,5vw,48px);border:1px solid var(--v5-stroke);border-radius:31px;background:radial-gradient(circle at 85% 10%,rgba(242,215,125,.26),transparent 28%),linear-gradient(135deg,#17402a,#07150e 62%,#030806);box-shadow:0 26px 70px rgba(0,0,0,.38)}
    .arena-page-hero:after{content:"BDA";position:absolute;right:-18px;bottom:-75px;color:rgba(255,255,255,.035);font:900 220px/1 "Barlow Condensed",sans-serif}.arena-page-hero>div,.arena-page-hero>aside{position:relative;z-index:1}.arena-page-hero h1{margin:8px 0 10px;font-size:clamp(55px,10vw,104px);line-height:.78;text-transform:uppercase}.arena-page-hero p{max-width:650px;margin:0;color:#d2ddd5;font-size:13px;line-height:1.55}.arena-page-hero aside{display:grid;justify-items:end}.arena-page-hero aside>b{color:var(--gold-soft);font:900 72px/.8 "Barlow Condensed",sans-serif}.arena-page-hero aside>span{margin:8px 0 16px;color:var(--muted);font-size:8px;font-weight:900;text-transform:uppercase}.arena-page-hero aside button{min-height:45px}
    .arena-catalog-head{display:flex;align-items:end;justify-content:space-between;gap:16px;margin:24px 0 12px}.arena-catalog-head h2{margin:4px 0 0;font-size:34px;text-transform:uppercase}.arena-catalog-head .arena-toolbar{margin:0;padding:5px;border:1px solid var(--line);border-radius:15px;background:rgba(3,8,6,.55)}.arena-catalog-head .arena-filter{border:0;border-radius:10px}
    .arena-grid{grid-template-columns:repeat(3,minmax(0,1fr))!important;gap:12px!important}.arena-card{min-height:340px!important;border-radius:24px!important;background:linear-gradient(155deg,var(--v5-panel),var(--v5-panel-deep))!important;transition:transform .18s,border-color .18s,box-shadow .18s}.arena-card:hover{transform:translateY(-4px);border-color:rgba(242,215,125,.34)!important;box-shadow:0 22px 50px rgba(0,0,0,.34)!important}.arena-card .arena-cover{height:150px!important}.arena-cover-edition{position:absolute;left:11px;bottom:10px;z-index:2;color:#fff;font-size:8px;font-weight:900;text-transform:uppercase}.arena-card .arena-body{display:grid;align-content:start;min-height:190px;padding:16px!important}.arena-card .arena-body h3{margin:0;font-size:28px!important}.arena-card .arena-body>p{display:-webkit-box;min-height:36px;margin:8px 0 12px;overflow:hidden;color:var(--muted);font-size:9px;line-height:1.5;-webkit-box-orient:vertical;-webkit-line-clamp:2}.arena-card .arena-meta{margin:0!important}.arena-card .arena-meta span{display:grid;gap:2px}.arena-card .arena-meta b{color:var(--text);font-size:9px}.arena-capacity{overflow:hidden;height:4px;margin:11px 0;border-radius:999px;background:rgba(255,255,255,.07)}.arena-capacity span{display:block;height:100%;border-radius:inherit;background:linear-gradient(90deg,var(--green),var(--gold-soft))}.arena-card .arena-open{display:flex;align-items:center;justify-content:space-between;margin-top:auto!important;border-color:rgba(242,215,125,.18)!important}.arena-card .arena-open b{font-size:14px}
    .arena-detail{scroll-margin-top:90px}.arena-detail-nav{position:sticky;top:68px;z-index:31;display:flex;align-items:center;justify-content:space-between;gap:12px;margin:24px 0 10px;padding:8px 9px 8px 13px;border:1px solid var(--line);border-radius:17px;background:linear-gradient(180deg,#09170f,#030906)!important;box-shadow:0 15px 35px rgba(0,0,0,.34);backdrop-filter:none}.arena-detail-nav>span{overflow:hidden;font-size:10px;font-weight:900;text-overflow:ellipsis;white-space:nowrap}.arena-detail-nav nav{display:flex;gap:5px}.arena-detail-nav button{min-height:35px;padding:0 10px;border:0;border-radius:10px;color:var(--muted);background:transparent;font-size:8px;font-weight:900;text-transform:uppercase}.arena-detail-nav button:hover{color:var(--gold-soft);background:rgba(242,215,125,.08)}
    .arena-detail .arena-hero{min-height:390px!important;padding:clamp(24px,5vw,48px)!important;border-radius:30px!important}.arena-detail .arena-hero:after{background:linear-gradient(180deg,rgba(2,7,4,.12),rgba(2,7,4,.94) 83%)!important}.arena-hero-copy{max-width:790px}.arena-hero-labels{display:flex;flex-wrap:wrap;gap:6px}.arena-hero-labels span{padding:6px 8px;border:1px solid rgba(255,255,255,.16);border-radius:999px;color:#fff;background:rgba(3,8,6,.38);font-size:8px;font-weight:900;text-transform:uppercase}.arena-hero-symbol{display:block;margin:18px 0 8px;font-size:42px}.arena-detail .arena-hero-copy h2{font-size:clamp(52px,10vw,96px)!important;letter-spacing:-.03em}.arena-detail .arena-hero-copy p{max-width:700px;font-size:13px!important}
    .arena-detail .arena-stats{grid-template-columns:1.1fr 1.4fr .8fr 1fr!important;gap:8px!important}.arena-detail .arena-stat{min-height:95px;display:grid;align-content:center;padding:14px 16px!important;border-radius:18px!important}.arena-detail .arena-stat span{order:-1;margin-bottom:6px}.arena-detail .arena-stat b{font-size:23px!important;line-height:1}.arena-detail .arena-stat small{color:var(--muted);font-size:11px}
    .arena-club-section{scroll-margin-top:135px;padding:20px;margin-top:12px;border:1px solid var(--line);border-radius:24px;background:linear-gradient(150deg,var(--v5-panel),var(--v5-panel-deep))}.arena-club-section .section-head{margin:0 0 12px}.arena-clubs{display:grid!important;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px!important}.arena-club{display:flex;align-items:center;gap:8px!important;min-height:44px;padding:8px 10px!important;border-radius:13px!important}.arena-club i{display:grid;place-items:center;width:25px;height:25px;flex:0 0 auto;border-radius:8px;color:var(--gold-soft);background:rgba(242,215,125,.09);font-style:normal;font-size:8px;font-weight:900}#tournamentCompetition{scroll-margin-top:135px}.arena-legacy{border-top:0!important}
    #giManager{padding-top:2px}.gi-head{min-height:165px;align-items:end!important;padding:24px!important}.gi-head h2{font-size:clamp(42px,7vw,68px)!important}.gi-metrics{gap:9px!important}.gi-metrics>div{min-height:80px!important;display:grid;align-content:center!important}.gi-content{margin-top:16px!important}.gi-game{transition:transform .16s,border-color .16s}.gi-game:hover{transform:translateY(-2px);border-color:rgba(242,215,125,.30)!important}

    @media(max-width:900px){#arenaStats{grid-template-columns:repeat(3,1fr)}.home-command nav{grid-template-columns:repeat(2,1fr)}.arena-grid{grid-template-columns:repeat(2,minmax(0,1fr))!important}.arena-page-hero{grid-template-columns:1fr}.arena-page-hero aside{justify-items:start}.arena-detail .arena-stats{grid-template-columns:repeat(2,1fr)!important}.arena-clubs{grid-template-columns:repeat(2,minmax(0,1fr))}.arena-catalog-head{align-items:stretch;flex-direction:column}.arena-catalog-head .arena-toolbar{width:100%}}
    @media(max-width:620px){[data-page="home"]{gap:12px}[data-page="home"] .hero{min-height:460px;padding:24px 18px;border-radius:25px}[data-page="home"] .hero:after{right:22px;top:25px;width:105px;border-radius:26px}[data-page="home"] .hero h1{font-size:clamp(52px,17vw,78px)}[data-page="home"] .hero p{font-size:12px}[data-page="home"] .hero-actions{display:grid;grid-template-columns:1fr 1fr}[data-page="home"] .hero-actions button{width:100%;padding:0 10px}#arenaStats{grid-template-columns:repeat(2,1fr)}#arenaStats .stat{min-height:79px}.home-command{padding:14px;border-radius:21px}.home-command>header{align-items:start;flex-direction:column}.home-command nav{grid-template-columns:1fr}.home-command nav button{min-height:69px}.arena-home-scroll{grid-auto-columns:86%!important}.arena-page-hero{min-height:330px;padding:26px 18px;border-radius:25px}.arena-page-hero h1{font-size:58px}.arena-page-hero aside>b{font-size:58px}.arena-grid{grid-template-columns:1fr!important}.arena-card{min-height:320px!important}.arena-card .arena-cover{height:145px!important}.arena-detail-nav{top:61px;align-items:flex-start;flex-direction:column}.arena-detail-nav nav{width:100%;overflow-x:auto}.arena-detail-nav button{flex:1;white-space:nowrap}.arena-detail .arena-hero{min-height:420px!important;padding:25px 18px!important}.arena-detail .arena-hero-copy h2{font-size:55px!important}.arena-detail .arena-stats{grid-template-columns:1fr 1fr!important}.arena-detail .arena-stat{min-height:83px}.arena-detail .arena-stat b{font-size:18px!important}.arena-club-section{padding:14px}.arena-clubs{grid-template-columns:1fr}.gi-head{min-height:145px;padding:18px!important}}
    @media(max-width:390px){[data-page="home"] .hero-actions{grid-template-columns:1fr}.arena-detail .arena-stats{grid-template-columns:1fr!important}.arena-page-hero h1,.arena-detail .arena-hero-copy h2{font-size:48px!important}}
    @media(prefers-reduced-motion:reduce){.arena-card,.home-command nav button,.gi-game{transition:none!important}.arena-card:hover,.home-command nav button:hover,.gi-game:hover{transform:none!important}}
  `;
  document.head.append(style);
  setTimeout(() => document.head.append(style), 0);
})();


/* ===== painel-visual-fix.js ===== */
(() => {
  'use strict';

  const style = document.createElement('style');
  style.textContent = `
    .top-actions #cloudPanelBtn.cloud-panel-trigger{
      opacity:1!important;
      visibility:visible;
      color:#171107!important;
      border:1px solid #f5dc86!important;
      background:linear-gradient(135deg,#fff2b5 0%,#f5dc86 42%,#d3a93a 100%)!important;
      box-shadow:0 10px 28px rgba(211,169,58,.34),inset 0 1px 0 rgba(255,255,255,.72)!important;
      filter:none!important;
      font-weight:900!important;
      letter-spacing:.055em!important;
      text-shadow:none!important;
    }

    .top-actions #cloudPanelBtn.cloud-panel-trigger:not([hidden]){
      display:inline-flex!important;
      align-items:center;
      justify-content:center;
      gap:6px;
    }

    .top-actions #cloudPanelBtn.cloud-panel-trigger:not([hidden])::before{
      content:"⚙";
      font-size:14px;
      line-height:1;
    }

    .top-actions #cloudPanelBtn.cloud-panel-trigger:hover{
      color:#100c04!important;
      border-color:#fff0a4!important;
      background:linear-gradient(135deg,#fff7d2 0%,#f9e394 44%,#dcb33f 100%)!important;
      box-shadow:0 14px 34px rgba(211,169,58,.44),0 0 0 3px rgba(245,220,134,.10)!important;
      transform:translateY(-2px)!important;
    }

    .top-actions #cloudPanelBtn.cloud-panel-trigger:focus-visible{
      outline:3px solid rgba(245,220,134,.34)!important;
      outline-offset:3px!important;
    }

    @media(max-width:520px){
      .top-actions #cloudPanelBtn.cloud-panel-trigger{
        min-height:38px!important;
        padding:0 10px!important;
        font-size:9px!important;
      }
      .top-actions #cloudPanelBtn.cloud-panel-trigger:not([hidden])::before{font-size:12px}
    }
  `;
  document.head.append(style);

  const reinforce = () => {
    const button = document.getElementById('cloudPanelBtn');
    if (!button) return;
    button.classList.add('cloud-panel-visible-fix');
    button.setAttribute('title', 'Abrir painel administrativo');
  };

  reinforce();
  window.ArenaDOMEvents.subscribe(reinforce, { selector: '#adminPanel,.admin-panel,[data-admin]' });
})();


/* ===== arena-design-v3.js ===== */
(() => {
  'use strict';

  const $ = (selector, root = document) => root.querySelector(selector);
  const $$ = (selector, root = document) => [...root.querySelectorAll(selector)];

  document.documentElement.dataset.arenaDesign = 'v4';
  document.body.classList.add('arena-design-v3', 'arena-design-v4');

  function improveBrand() {
    const strong = $('.brand-copy strong');
    const subtitle = $('.brand-copy span');
    if (strong) strong.textContent = 'Arena BDA';
    if (subtitle) subtitle.textContent = 'Competições oficiais do Clã';
  }

  function improveNavigation() {
    $$('.bottom-nav .nav-btn').forEach(button => {
      if (!button.title) button.title = button.textContent.trim();
      if (!button.hasAttribute('aria-label')) button.setAttribute('aria-label', button.textContent.trim());
    });
  }

  function enhanceHome() {
    const home = $('[data-page="home"]');
    const hero = $('.hero', home);
    const content = $('.hero-content', hero);
    if (!home || !hero || !content) return;

    hero.classList.add('arena-hero-v4');
    $('.eyebrow', content).textContent = 'Arena BDA';
    $('h1', content).textContent = 'Competições do Clã BDA';
    $('p', content).textContent = 'Calendário, resultados, classificações e histórico oficial dos campeonatos do clã.';

    const primary = $('.hero-actions .primary', content);
    const secondary = $('.hero-actions .secondary', content);
    if (primary && !primary.querySelector('span')) {
      primary.innerHTML = '<span>Ver campeonatos</span><i aria-hidden="true">→</i>';
    }
    if (secondary && !secondary.querySelector('span')) {
      secondary.innerHTML = '<span>Ver clubes</span><i aria-hidden="true">↗</i>';
    }

    const existingLayout = $('.arena-hero-layout', hero);
    const stats = $('.quick-stats', home);
    if (existingLayout) {
      hero.insertBefore(content, existingLayout);
      existingLayout.remove();
    }
    $('.arena-hero-console', hero)?.remove();
    if (stats) {
      stats.classList.remove('arena-console-stats');
      hero.after(stats);
    }

    $('.home-grid', home)?.classList.add('arena-home-dashboard');
  }

  function improveAccessibility(root = document) {
    $$('img:not([decoding])', root).forEach(image => { image.decoding = 'async'; });
    $$('button:not([type])', root).forEach(button => { button.type = 'button'; });
    $$('input,select,textarea', root).forEach(field => {
      if (!field.autocomplete && field.type !== 'password') field.autocomplete = 'off';
    });
  }

  function markSections(root = document) {
    $$('.page', root).forEach(page => page.classList.add('arena-page-v3'));
    $$('.card,.form-card,.admin-panel,.live-card,.arena-card,.team-card,.champion-card', root)
      .forEach(card => card.classList.add('arena-surface-v3'));
  }

  function refresh(root = document) {
    improveBrand();
    improveNavigation();
    enhanceHome();
    improveAccessibility(root);
    markSections(root);
  }

  const style = document.createElement('style');
  style.id = 'arenaDesignV3Styles';
  style.textContent = `
    :root{
      --arena-bg-0:#020503;
      --arena-bg-1:#06100a;
      --arena-bg-2:#0a1810;
      --arena-panel:rgba(10,24,16,.88);
      --arena-panel-strong:rgba(15,34,23,.96);
      --arena-panel-soft:rgba(255,255,255,.035);
      --arena-border:rgba(221,241,227,.11);
      --arena-border-strong:rgba(245,220,134,.32);
      --arena-text:#f6faf7;
      --arena-muted:#a7b8ad;
      --arena-gold:#d6aa39;
      --arena-gold-soft:#f6df91;
      --arena-green:#5be19a;
      --arena-danger:#ff7987;
      --arena-radius-xl:28px;
      --arena-radius-lg:21px;
      --arena-radius-md:15px;
      --arena-shadow-xl:0 32px 90px rgba(0,0,0,.44);
      --arena-shadow-lg:0 18px 46px rgba(0,0,0,.28);
      --arena-ease:cubic-bezier(.2,.8,.2,1);
    }

    html{background:var(--arena-bg-0)}
    body.arena-design-v3{
      min-height:100dvh;
      color:var(--arena-text);
      background:
        radial-gradient(circle at 82% -5%,rgba(246,223,145,.14),transparent 30%),
        radial-gradient(circle at 10% 28%,rgba(91,225,154,.07),transparent 25%),
        linear-gradient(180deg,var(--arena-bg-2),var(--arena-bg-0) 58%)!important;
    }
    body.arena-design-v3:before{
      opacity:.21!important;
      background-size:54px 54px!important;
    }

    .app-shell{
      width:min(100%,1280px)!important;
      max-width:1280px!important;
      padding-inline:clamp(10px,2vw,22px);
    }

    .topbar{
      top:10px!important;
      z-index:120!important;
      width:100%;
      min-height:70px!important;
      margin:10px auto 0!important;
      padding:10px 12px!important;
      border:1px solid var(--arena-border)!important;
      border-radius:22px!important;
      background:linear-gradient(180deg,rgba(10,24,16,.94),rgba(4,10,7,.90))!important;
      box-shadow:0 18px 50px rgba(0,0,0,.34),inset 0 1px 0 rgba(255,255,255,.035)!important;
      backdrop-filter:blur(20px) saturate(1.12)!important;
    }
    .topbar:after{left:26px!important;right:26px!important;opacity:.34!important}
    .brand{gap:12px!important}
    .brand-mark{
      width:49px!important;height:49px!important;border-radius:16px!important;
      box-shadow:0 10px 24px rgba(0,0,0,.34),0 0 0 1px rgba(246,223,145,.08)!important;
    }
    .brand-copy strong{font-size:24px!important;letter-spacing:.055em!important}
    .brand-copy span{margin-top:4px!important;color:#aebdb3!important;font-size:8px!important;letter-spacing:.13em!important}
    .arena-top-status{min-height:36px!important;background:rgba(255,255,255,.027)!important}
    .top-actions{gap:6px!important}
    .top-actions button{min-height:40px!important}

    main{
      width:100%;
      padding:24px 4px 10px!important;
    }
    .arena-page-v3{min-height:55vh}
    .page.active{animation:arenaV3Page .28s var(--arena-ease)}
    @keyframes arenaV3Page{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:none}}

    .hero,.arena-hero,.rank-hero,.history-hero,.league-generator-head{
      overflow:hidden!important;
      border:1px solid var(--arena-border-strong)!important;
      border-radius:var(--arena-radius-xl)!important;
      box-shadow:var(--arena-shadow-xl),inset 0 1px 0 rgba(255,255,255,.045)!important;
    }
    .hero{
      min-height:310px!important;
      padding:clamp(22px,4vw,38px)!important;
      background:
        linear-gradient(180deg,transparent 0 22%,rgba(2,7,4,.92) 88%),
        radial-gradient(circle at 82% 17%,rgba(246,223,145,.35),transparent 20%),
        linear-gradient(135deg,#173c27,#07120b 68%)!important;
    }
    .hero h1{max-width:850px;font-size:clamp(43px,7.2vw,74px)!important;line-height:.9!important;text-wrap:balance}
    .hero p{max-width:650px!important;color:#ced9d1!important;font-size:13px!important;line-height:1.65!important}
    .hero-actions{gap:8px!important;margin-top:20px!important}

    .section-head{
      align-items:center!important;
      margin:30px 3px 13px!important;
      padding-left:14px;
      border-left:3px solid var(--arena-gold-soft);
    }
    .section-head h2{font-size:clamp(25px,4vw,32px)!important;line-height:1!important;letter-spacing:.015em!important}
    .section-head p{margin-top:5px!important;font-size:11px!important;line-height:1.45!important}
    .section-head>button{min-height:38px!important;padding:0 11px!important;border:1px solid var(--arena-border)!important;border-radius:11px!important;background:rgba(255,255,255,.03)!important}

    .arena-surface-v3,.card,.form-card,.admin-panel{
      border-color:var(--arena-border)!important;
      background:linear-gradient(155deg,rgba(17,37,25,.91),rgba(7,17,11,.94))!important;
      box-shadow:var(--arena-shadow-lg),inset 0 1px 0 rgba(255,255,255,.025)!important;
    }
    .card{border-radius:var(--arena-radius-lg)!important}
    .quick-stats{gap:12px!important;margin:16px 0!important}
    .stat{
      min-height:91px;
      display:grid;
      align-content:center;
      padding:17px 15px!important;
      border-color:var(--arena-border)!important;
      border-radius:18px!important;
      background:linear-gradient(145deg,rgba(20,44,29,.88),rgba(7,17,11,.94))!important;
      box-shadow:0 14px 34px rgba(0,0,0,.19)!important;
    }
    .stat b{font-size:31px!important}.stat span{font-size:8px!important}

    .primary,.secondary,.ghost,.danger,.admin-btn,.icon-btn{
      min-height:42px!important;
      border-radius:12px!important;
      transition:transform .16s var(--arena-ease),border-color .16s ease,background .16s ease,box-shadow .16s ease!important;
    }
    .primary{color:#171107!important;background:linear-gradient(135deg,#fff0b4,var(--arena-gold-soft) 44%,var(--arena-gold))!important}
    .secondary{color:var(--arena-text)!important;background:rgba(255,255,255,.055)!important;border-color:var(--arena-border)!important}
    .ghost{color:var(--arena-gold-soft)!important;background:rgba(246,223,145,.025)!important;border-color:rgba(246,223,145,.24)!important}
    .danger{color:#ffc0c6!important;background:rgba(255,121,135,.09)!important;border-color:rgba(255,121,135,.23)!important}
    button:hover{filter:brightness(1.05)}
    button:focus-visible,input:focus-visible,select:focus-visible,textarea:focus-visible{
      outline:3px solid rgba(246,223,145,.24)!important;
      outline-offset:2px!important;
    }

    input,select,textarea{
      min-height:45px;
      border:1px solid var(--arena-border)!important;
      border-radius:12px!important;
      color:var(--arena-text)!important;
      background:rgba(2,7,4,.66)!important;
      box-shadow:inset 0 1px 0 rgba(255,255,255,.025);
    }
    textarea{min-height:105px}
    label{color:#aebdb3!important;font-size:9px!important}
    .form-card{padding:18px!important;border-radius:22px!important}
    .form-grid{gap:13px!important}

    .team-grid,.champion-grid,.arena-grid{gap:14px!important}
    .team-card,.champion-card,.arena-card{transition:transform .18s var(--arena-ease),border-color .18s ease,box-shadow .18s ease}
    .team-card:hover,.champion-card:hover,.arena-card:hover{transform:translateY(-3px);border-color:rgba(246,223,145,.25)!important;box-shadow:0 22px 52px rgba(0,0,0,.30)!important}
    .team-card{padding:16px!important}
    .team-card h3{font-size:20px!important}
    .team-meta{padding-top:12px!important}
    .team-mini-badge{width:48px!important;height:48px!important}

    .bottom-nav{
      left:50%!important;
      bottom:12px!important;
      width:min(calc(100% - 20px),760px)!important;
      min-height:70px!important;
      gap:5px;
      padding:7px!important;
      border:1px solid rgba(221,241,227,.13)!important;
      border-radius:22px!important;
      background:rgba(7,17,11,.92)!important;
      box-shadow:0 22px 60px rgba(0,0,0,.46),inset 0 1px 0 rgba(255,255,255,.035)!important;
      backdrop-filter:blur(20px) saturate(1.16)!important;
    }
    .nav-btn{
      position:relative;
      min-height:54px;
      border-radius:15px!important;
      color:#92a49a!important;
      transition:background .16s ease,color .16s ease,transform .16s var(--arena-ease)!important;
    }
    .nav-btn i{font-size:20px!important}
    .nav-btn.active{
      color:#171107!important;
      background:linear-gradient(135deg,#fff0b4,var(--arena-gold-soft),var(--arena-gold))!important;
      box-shadow:0 9px 22px rgba(214,170,57,.22)!important;
    }
    .nav-btn:active{transform:scale(.97)}

    .modal-backdrop{
      padding:16px!important;
      background:rgba(0,0,0,.80)!important;
      backdrop-filter:blur(10px)!important;
    }
    .modal{
      width:min(100%,560px)!important;
      max-height:calc(100dvh - 32px)!important;
      padding:21px!important;
      border:1px solid var(--arena-border-strong)!important;
      border-radius:25px!important;
      background:linear-gradient(160deg,#153322,#07110b)!important;
      box-shadow:0 36px 100px rgba(0,0,0,.68)!important;
    }

    .match-list{gap:11px!important}
    .match-row{padding:14px!important;border-radius:15px;background:rgba(255,255,255,.022)}
    .fixture{border-radius:15px!important;background:rgba(2,7,4,.56)!important}

    [data-page="community"] .message{border-radius:8px 18px 18px 18px!important;background:linear-gradient(145deg,rgba(18,39,26,.92),rgba(7,17,11,.95))!important}
    [data-page="community"] .message.mine{border-radius:18px 8px 18px 18px!important;background:rgba(246,223,145,.10)!important}
    .chat-compose{border-radius:18px!important;background:rgba(7,17,11,.88)!important;backdrop-filter:blur(12px)}

    @media(min-width:920px){
      main{padding-bottom:96px!important}
      .bottom-nav{grid-template-columns:repeat(5,minmax(100px,1fr))!important}
      .nav-btn{grid-template-columns:auto 1fr;place-items:center start;justify-content:center;gap:8px;padding:0 13px!important;font-size:10px!important}
      .nav-btn i{font-size:18px!important}
    }

    @media(max-width:760px){
      .app-shell{padding-inline:8px}
      .topbar{top:5px!important;margin-top:5px!important;border-radius:18px!important}
      .arena-top-status{display:none!important}
      main{padding-top:18px!important}
      .hero{min-height:285px!important;border-radius:24px!important}
      .quick-stats{grid-template-columns:repeat(3,minmax(0,1fr))!important;gap:8px!important}
      .stat{min-height:82px;padding:13px 9px!important}.stat b{font-size:27px!important}
      .team-grid,.champion-grid,.arena-grid{grid-template-columns:1fr!important}
      .section-head{align-items:flex-start!important}
      .bottom-nav{bottom:7px!important;width:calc(100% - 14px)!important;min-height:67px!important;border-radius:20px!important}
    }

    @media(max-width:480px){
      body{padding-bottom:calc(var(--nav-h) + env(safe-area-inset-bottom) + 12px)!important}
      .brand-copy span{display:none!important}
      .brand-mark{width:44px!important;height:44px!important}
      .brand-copy strong{font-size:21px!important}
      .top-actions .icon-btn{width:39px!important}
      .hero{min-height:265px!important;padding:20px!important}
      .hero h1{font-size:clamp(39px,13vw,58px)!important}
      .hero-actions>*{flex:1 1 140px}
      .section-head{margin-top:25px!important;padding-left:11px}
      .section-head h2{font-size:26px!important}
      .quick-stats{grid-template-columns:1fr 1fr!important}
      .quick-stats .stat:last-child:nth-child(odd){grid-column:1/-1}
      .nav-btn{font-size:8px!important}.nav-btn i{font-size:19px!important}
      .modal-backdrop{place-items:end center!important;padding:8px!important}
      .modal{max-height:calc(100dvh - 16px)!important;padding:17px!important;border-radius:22px 22px 14px 14px!important}
    }

    @media(prefers-reduced-motion:reduce){
      *,*::before,*::after{scroll-behavior:auto!important;animation-duration:.01ms!important;animation-iteration-count:1!important;transition-duration:.01ms!important}
    }

    /* Arena BDA Design V4 */
    body.arena-design-v4{
      --arena-bg-0:#020604;
      --arena-bg-1:#06100a;
      --arena-panel:rgba(9,21,14,.92);
      --arena-border:rgba(231,244,235,.105);
      --arena-border-strong:rgba(242,215,125,.30);
      background:
        radial-gradient(ellipse at 72% -8%,rgba(242,215,125,.13),transparent 33%),
        radial-gradient(ellipse at 6% 42%,rgba(70,207,135,.055),transparent 28%),
        linear-gradient(180deg,#09150e 0,#030805 48%,#020503 100%)!important;
    }
    body.arena-design-v4:before{opacity:.12!important;background-size:64px 64px!important}
    body.arena-design-v4 .app-shell{width:min(100%,1380px)!important;max-width:1380px!important}
    body.arena-design-v4 main{padding-top:20px!important}

    .arena-design-v4 .topbar{
      min-height:68px!important;
      border-color:rgba(231,244,235,.09)!important;
      background:linear-gradient(180deg,#0a1810,#030906)!important;
      box-shadow:0 16px 42px rgba(0,0,0,.25)!important;
      backdrop-filter:none!important;
      -webkit-backdrop-filter:none!important;
    }
    .arena-design-v4 .brand-mark{border-radius:14px!important}
    .arena-design-v4 .brand-copy strong{font-size:22px!important;letter-spacing:.045em!important}
    .arena-design-v4 .brand-copy span{letter-spacing:.11em!important}

    .arena-design-v4 .arena-hero-v4{
      min-height:0!important;
      padding:0!important;
      border-color:rgba(242,215,125,.25)!important;
      background:
        radial-gradient(circle at 76% 15%,rgba(242,215,125,.16),transparent 24%),
        linear-gradient(135deg,rgba(24,58,39,.96),rgba(5,14,9,.99) 66%)!important;
    }
    .arena-design-v4 .arena-hero-v4:before{
      content:"";
      position:absolute;
      inset:0;
      opacity:.25;
      background:
        linear-gradient(115deg,transparent 0 57%,rgba(242,215,125,.055) 57.2%,transparent 57.5%),
        linear-gradient(rgba(255,255,255,.025) 1px,transparent 1px),
        linear-gradient(90deg,rgba(255,255,255,.025) 1px,transparent 1px);
      background-size:auto,36px 36px,36px 36px;
      mask-image:linear-gradient(90deg,black,transparent 82%);
      transform:none;
      filter:none;
    }
    .arena-design-v4 .arena-hero-v4:after{
      inset:auto -80px -150px auto;
      width:420px;
      height:420px;
      border:1px solid rgba(242,215,125,.10);
      border-radius:50%;
      background:radial-gradient(circle,rgba(242,215,125,.06),transparent 66%);
    }
    .arena-hero-layout{
      position:relative;
      z-index:2;
      display:grid;
      grid-template-columns:minmax(0,1.25fr) minmax(310px,.75fr);
      align-items:stretch;
      gap:18px;
      width:100%;
      padding:clamp(20px,3vw,34px);
    }
    .arena-design-v4 .arena-hero-v4 .hero-content{
      display:flex;
      flex-direction:column;
      align-items:flex-start;
      justify-content:center;
      max-width:760px;
      min-height:330px;
      padding:10px clamp(0px,1vw,12px);
    }
    .arena-design-v4 .arena-hero-v4 .eyebrow{
      display:inline-flex;
      align-items:center;
      gap:8px;
      padding:7px 10px;
      border:1px solid rgba(242,215,125,.21);
      border-radius:999px;
      background:rgba(242,215,125,.055);
      font-size:9px;
      letter-spacing:.14em;
    }
    .arena-design-v4 .arena-hero-v4 h1{
      max-width:760px;
      margin:17px 0 12px!important;
      font-size:clamp(48px,6.7vw,82px)!important;
      line-height:.86!important;
      letter-spacing:-.025em!important;
    }
    .arena-design-v4 .arena-hero-v4 p{
      max-width:620px!important;
      color:#c5d2c9!important;
      font-size:13px!important;
      line-height:1.7!important;
    }
    .arena-design-v4 .hero-actions{width:100%;margin-top:23px!important}
    .arena-design-v4 .hero-actions button{
      display:inline-flex;
      align-items:center;
      justify-content:space-between;
      gap:18px;
      min-height:48px!important;
      padding:0 18px!important;
      border-radius:13px!important;
    }
    .arena-design-v4 .hero-actions button i{font-size:17px;font-style:normal}

    .arena-hero-console{
      align-self:stretch;
      display:flex;
      flex-direction:column;
      min-width:0;
      padding:16px;
      border:1px solid rgba(231,244,235,.10);
      border-radius:22px;
      background:linear-gradient(160deg,rgba(10,25,16,.88),rgba(3,9,6,.92));
      box-shadow:inset 0 1px 0 rgba(255,255,255,.035),0 20px 55px rgba(0,0,0,.22);
      backdrop-filter:blur(18px);
    }
    .arena-hero-console>header{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:10px;
      padding:1px 2px 13px;
      border-bottom:1px solid var(--arena-border);
    }
    .arena-hero-console>header span{
      display:flex;
      align-items:center;
      gap:7px;
      color:#dfe9e2;
      font-size:8px;
      font-weight:900;
      letter-spacing:.11em;
      text-transform:uppercase;
    }
    .arena-hero-console>header span i{
      width:7px;
      height:7px;
      border-radius:50%;
      background:var(--arena-green);
      box-shadow:0 0 0 4px rgba(91,225,154,.09),0 0 14px rgba(91,225,154,.42);
    }
    .arena-hero-console>header small{color:var(--arena-muted);font-size:7px}
    .arena-console-brand{
      display:flex;
      align-items:center;
      gap:11px;
      padding:15px 2px 13px;
    }
    .arena-console-brand img{
      width:45px;
      height:45px;
      border:1px solid rgba(242,215,125,.36);
      border-radius:13px;
      object-fit:cover;
      background:#020503;
    }
    .arena-console-brand small,.arena-console-brand b{display:block}
    .arena-console-brand small{color:var(--arena-gold-soft);font-size:7px;font-weight:900;letter-spacing:.13em}
    .arena-console-brand b{margin-top:5px;font:800 20px/1 "Barlow Condensed",sans-serif;text-transform:uppercase}
    .arena-console-stats{
      flex:1;
      display:grid!important;
      grid-template-columns:repeat(2,minmax(0,1fr))!important;
      gap:7px!important;
      margin:0!important;
    }
    .arena-console-stats .stat{
      min-height:76px!important;
      padding:12px!important;
      border-color:rgba(231,244,235,.085)!important;
      border-radius:14px!important;
      background:rgba(255,255,255,.026)!important;
      box-shadow:none!important;
    }
    .arena-console-stats .stat b{font-size:26px!important}
    .arena-console-stats .stat span{margin-top:6px!important;font-size:7px!important;letter-spacing:.11em!important}

    .arena-design-v4 .section-head{
      min-height:50px;
      margin:34px 2px 14px!important;
      padding-left:0;
      border-left:0;
    }
    .arena-design-v4 .section-head h2{
      font-size:clamp(26px,3vw,34px)!important;
      letter-spacing:.005em!important;
      text-transform:uppercase;
    }
    .arena-design-v4 .section-head p{color:#92a59a!important;font-size:10px!important}
    .arena-design-v4 .section-head>button{
      padding:0 14px!important;
      border-color:rgba(242,215,125,.18)!important;
      color:var(--arena-gold-soft)!important;
    }

    .arena-design-v4 .arena-home-dashboard{gap:16px!important}
    .arena-design-v4 .arena-home-dashboard>div{
      min-width:0;
      padding:0;
      border-radius:22px;
    }
    .arena-design-v4 .live-card,.arena-design-v4 .match-list{
      overflow:hidden;
      border-color:rgba(231,244,235,.09)!important;
      background:rgba(7,17,11,.86)!important;
      box-shadow:0 18px 45px rgba(0,0,0,.22)!important;
    }
    .arena-design-v4 .live-top{padding:14px 16px!important;background:rgba(255,255,255,.018)}
    .arena-design-v4 .versus{min-height:190px;padding:24px 18px!important}
    .arena-design-v4 .club-badge{width:64px!important;height:64px!important;border-radius:18px!important}
    .arena-design-v4 .scorebox b{color:var(--arena-gold-soft);font-size:30px!important}
    .arena-design-v4 .match-row{
      min-height:72px;
      padding:13px 15px!important;
      border-radius:0!important;
      background:transparent!important;
    }
    .arena-design-v4 .match-row+.match-row{border-top:1px solid rgba(231,244,235,.07)}
    .arena-design-v4 .match-stage{
      display:inline-flex;
      width:max-content;
      padding:5px 7px;
      border:1px solid rgba(242,215,125,.16);
      border-radius:7px;
      background:rgba(242,215,125,.04);
    }
    .arena-design-v4 .match-status{
      padding:5px 7px;
      border-radius:999px;
      background:rgba(91,225,154,.07);
      color:#8fd9af;
      font-size:8px;
    }

    .arena-design-v4 .arena-surface-v3,.arena-design-v4 .card,.arena-design-v4 .form-card,.arena-design-v4 .admin-panel{
      border-color:rgba(231,244,235,.09)!important;
      background:linear-gradient(155deg,rgba(12,28,18,.90),rgba(5,13,8,.94))!important;
      box-shadow:0 16px 42px rgba(0,0,0,.20),inset 0 1px 0 rgba(255,255,255,.02)!important;
    }
    .arena-design-v4 .team-grid,.arena-design-v4 .champion-grid,.arena-design-v4 .arena-grid{gap:12px!important}
    .arena-design-v4 .team-card,.arena-design-v4 .champion-card,.arena-design-v4 .arena-card{border-radius:18px!important}
    .arena-design-v4 .primary{box-shadow:0 10px 28px rgba(214,170,57,.16)!important}
    .arena-design-v4 button:active{transform:scale(.98)}

    @media(min-width:981px){
      body.arena-design-v4.arena-navigation-ready{padding-left:244px!important}
      .arena-design-v4 .bottom-nav.arena-side-nav{
        left:12px!important;
        top:12px!important;
        bottom:12px!important;
        width:216px!important;
        padding:12px!important;
        border-color:rgba(231,244,235,.10)!important;
        border-radius:22px!important;
        background:linear-gradient(180deg,rgba(8,20,13,.98),rgba(3,9,6,.99))!important;
        box-shadow:0 24px 65px rgba(0,0,0,.40),inset 0 1px 0 rgba(255,255,255,.025)!important;
      }
      .arena-design-v4 .arena-side-brand{border-bottom-color:rgba(231,244,235,.08)!important}
      .arena-design-v4 .arena-side-logo{border-radius:13px!important;box-shadow:none!important}
      .arena-design-v4 .arena-side-nav .nav-btn{
        min-height:52px!important;
        border-radius:13px!important;
      }
      .arena-design-v4 .arena-side-nav .nav-btn.active{
        color:var(--arena-gold-soft)!important;
        border-color:rgba(242,215,125,.18)!important;
        background:rgba(242,215,125,.075)!important;
        box-shadow:inset 3px 0 0 var(--arena-gold-soft)!important;
      }
      .arena-design-v4 .arena-side-nav .nav-btn.active i{
        color:#171107!important;
        border-color:rgba(242,215,125,.34)!important;
        background:linear-gradient(135deg,var(--arena-gold-soft),var(--arena-gold))!important;
      }
      .arena-design-v4 .arena-side-nav .nav-btn.active .arena-nav-copy small{color:#acbdb2!important}
      .arena-design-v4 .arena-side-nav .nav-btn.active:after{display:none!important}
      .arena-design-v4 .arena-side-footer{border-color:rgba(231,244,235,.08)!important;background:rgba(255,255,255,.02)!important}
    }
    @media(max-width:980px){
      .arena-design-v4 .arena-mobile-nav{
        left:8px;
        right:8px;
        bottom:7px;
        min-height:66px;
        border-color:rgba(231,244,235,.11);
        border-radius:20px;
        background:rgba(5,14,9,.96);
        box-shadow:0 18px 48px rgba(0,0,0,.46),inset 0 1px 0 rgba(255,255,255,.025);
      }
      .arena-design-v4 .arena-mobile-item{border-radius:13px}
      .arena-design-v4 .arena-mobile-item.active{
        box-shadow:0 8px 20px rgba(214,170,57,.14);
      }
    }

    @media(min-width:980px){
      .arena-design-v4 .arena-home-dashboard{
        grid-template-columns:minmax(0,1.35fr) minmax(330px,.65fr)!important;
        gap:18px!important;
      }
    }
    @media(max-width:900px){
      .arena-hero-layout{grid-template-columns:1fr}
      .arena-design-v4 .arena-hero-v4 .hero-content{min-height:270px}
      .arena-console-stats{grid-template-columns:repeat(3,minmax(0,1fr))!important}
    }
    @media(max-width:560px){
      .arena-design-v4 main{padding-inline:6px!important}
      .arena-hero-layout{gap:10px;padding:13px}
      .arena-design-v4 .arena-hero-v4{border-radius:22px!important}
      .arena-design-v4 .arena-hero-v4 .hero-content{min-height:290px;padding:13px 8px}
      .arena-design-v4 .arena-hero-v4 h1{font-size:clamp(43px,13.3vw,62px)!important}
      .arena-design-v4 .arena-hero-v4 p{font-size:12px!important;line-height:1.6!important}
      .arena-design-v4 .hero-actions button{flex:1 1 100%;width:100%}
      .arena-hero-console{padding:12px;border-radius:17px}
      .arena-console-brand{padding-block:12px 10px}
      .arena-console-stats{grid-template-columns:repeat(2,minmax(0,1fr))!important}
      .arena-console-stats .stat{min-height:67px!important}
      .arena-design-v4 .section-head{margin-top:27px!important}
      .arena-design-v4 .section-head h2{font-size:27px!important}
      .arena-design-v4 .match-row{grid-template-columns:58px 1fr!important;gap:8px!important}
      .arena-design-v4 .match-status{display:none}
      .arena-design-v4 .versus{min-height:170px;padding-inline:10px!important}
      .arena-design-v4 .club-badge{width:54px!important;height:54px!important}
    }
  `;
  document.head.append(style);

  refresh();

  const root = $('.app-shell') || document.body;
  const observer = new MutationObserver(mutations => {
    const added = mutations.flatMap(mutation => [...mutation.addedNodes]).filter(node => node.nodeType === 1);
    if (!added.length) return;
    requestAnimationFrame(() => added.forEach(node => refresh(node)));
  });
  observer.observe(root, { childList: true, subtree: true });

  window.ArenaBDADesignV3 = Object.freeze({ refresh });
})();


/* ===== topbar-mobile-clean.js ===== */
(() => {
  'use strict';

  const $ = (selector, root = document) => root.querySelector(selector);

  function forceDarkTheme() {
    document.documentElement.dataset.theme = 'dark';
    document.documentElement.style.colorScheme = 'dark';
    $('#arenaThemeToggle')?.remove();
    try { localStorage.removeItem('arena-bda-theme'); } catch {}
    const meta = $('meta[name="theme-color"]');
    if (meta) meta.content = '#07100c';
  }

  function isAdminActive() {
    try {
      if (window.ArenaBDAAuth?.isAdmin) return Boolean(window.ArenaBDAAuth.isAdmin());
    } catch {}
    const button = $('#adminBtn');
    return Boolean(
      document.documentElement.classList.contains('arena-admin-authenticated')
      || button?.classList.contains('active')
      || /ativo|sair/i.test(button?.textContent || '')
    );
  }

  function decorateTopbar() {
    forceDarkTheme();
    const topbar = $('.topbar');
    if (!topbar) return;
    topbar.classList.add('arena-topbar-clean');

    const panel = $('#cloudPanelBtn');
    if (panel && !panel.querySelector('.arena-clean-panel-icon')) {
      panel.classList.add('arena-clean-panel');
      panel.innerHTML = '<span class="arena-clean-panel-icon" aria-hidden="true">⚙</span><b>Painel</b>';
    }

    const admin = $('#adminBtn');
    if (admin) {
      const active = isAdminActive();
      const state = active ? 'active' : 'guest';
      if (admin.dataset.cleanAdminState !== state || !admin.querySelector('.arena-clean-admin-icon')) {
        admin.dataset.cleanAdminState = state;
        admin.classList.add('arena-clean-admin');
        admin.classList.toggle('active', active);
        admin.innerHTML = active
          ? '<i class="arena-clean-admin-dot" aria-hidden="true"></i><span class="arena-clean-admin-icon" aria-hidden="true">AD</span><b>Admin</b>'
          : '<span class="arena-clean-admin-icon" aria-hidden="true">AC</span><b>Entrar</b>';
        admin.setAttribute('aria-label', active ? 'Administrador ativo. Toque para sair' : 'Entrar como administrador');
      }
    }

    const grid = $('.arena-nav-sheet-grid');
    const share = $('#shareBtn');
    if (grid && share && !$('#arenaShareMore')) {
      const button = document.createElement('button');
      button.id = 'arenaShareMore';
      button.type = 'button';
      button.className = 'arena-sheet-item arena-share-more';
      button.innerHTML = '<i aria-hidden="true">↗</i><span><b>Compartilhar Arena</b><small>Enviar o link para outro membro</small></span><em>›</em>';
      button.addEventListener('click', () => {
        $('.arena-nav-sheet-backdrop')?.classList.remove('show');
        document.body.classList.remove('arena-sheet-open');
        share.click();
      });
      grid.append(button);
    }
  }

  const style = document.createElement('style');
  style.id = 'arenaTopbarCleanStyles';
  style.textContent = `
    .arena-topbar-clean .top-actions{min-width:0}
    .arena-clean-panel,.arena-clean-admin{display:inline-flex;align-items:center;justify-content:center;gap:6px;white-space:nowrap}
    .arena-clean-panel-icon,.arena-clean-admin-icon{font-size:15px;line-height:1}
    .arena-clean-panel b,.arena-clean-admin b{font-size:9px;line-height:1;text-transform:uppercase}
    .arena-clean-admin{position:relative}
    .arena-clean-admin-dot{position:absolute;right:5px;top:5px;width:7px;height:7px;border:2px solid #102018;border-radius:50%;background:var(--green)}
    #cloudPanelBtn[hidden]{display:none!important}
    @media(max-width:720px){
      .topbar.arena-topbar-clean{position:sticky!important;top:6px!important;z-index:48!important;min-height:58px!important;margin:6px 8px 12px!important;padding:6px 8px!important;gap:7px!important;border:1px solid rgba(242,215,125,.18)!important;border-radius:18px!important;background:rgba(6,16,11,.96)!important;box-shadow:0 10px 26px rgba(0,0,0,.28)!important;backdrop-filter:blur(14px)!important}
      .topbar.arena-topbar-clean .brand{width:44px!important;min-width:44px!important;flex:0 0 44px!important;margin:0!important;padding:0!important}
      .topbar.arena-topbar-clean .brand-copy,.topbar.arena-topbar-clean .brand:before,.topbar.arena-topbar-clean .brand:after{display:none!important}
      .topbar.arena-topbar-clean .brand-mark{width:44px!important;height:44px!important;margin:0!important;border-radius:13px!important;transform:none!important}
      .topbar.arena-topbar-clean .top-actions{flex:1 1 auto!important;display:flex!important;justify-content:flex-end!important;align-items:center!important;gap:5px!important}
      .topbar.arena-topbar-clean #shareBtn{display:none!important}
      .topbar.arena-topbar-clean #arenaNotificationsBtn{width:40px!important;min-width:40px!important;height:40px!important;min-height:40px!important;padding:0!important;border-radius:12px!important}
      .topbar.arena-topbar-clean #cloudPanelBtn{min-width:72px!important;height:40px!important;min-height:40px!important;padding:0 9px!important;border-radius:12px!important}
      .topbar.arena-topbar-clean #adminBtn{width:42px!important;min-width:42px!important;height:40px!important;min-height:40px!important;padding:0!important;border-radius:12px!important;font-size:0!important}
      .topbar.arena-topbar-clean #adminBtn b{display:none!important}
      .topbar.arena-topbar-clean #adminBtn:not(.active){color:var(--gold-soft)!important;background:rgba(255,255,255,.04)!important;border-color:var(--line)!important}
    }
    @media(max-width:370px){
      .topbar.arena-topbar-clean{margin-inline:5px!important;padding-inline:6px!important}
      .topbar.arena-topbar-clean .brand,.topbar.arena-topbar-clean .brand-mark{width:40px!important;min-width:40px!important;height:40px!important}
      .topbar.arena-topbar-clean #arenaNotificationsBtn,.topbar.arena-topbar-clean #adminBtn{width:38px!important;min-width:38px!important;height:38px!important;min-height:38px!important}
      .topbar.arena-topbar-clean #cloudPanelBtn{min-width:64px!important;height:38px!important;min-height:38px!important;padding-inline:6px!important}
    }
  `;
  document.head.append(style);

  window.ArenaBDAAuth?.subscribe?.(() => setTimeout(decorateTopbar, 0));
  window.addEventListener('arena:permissions-updated', decorateTopbar);
  window.ArenaBDATopbar = Object.freeze({ refresh: decorateTopbar });

  [0, 180, 600, 1400].forEach(delay => setTimeout(decorateTopbar, delay));
})();

;
